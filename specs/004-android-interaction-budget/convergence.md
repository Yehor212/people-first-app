# Android interaction implementation and open acceptance gates

## Exact-final RTL coverage, frame gate still open — 2026-09-09, 07:55 UTC

The same `bed4cf97` APK/PID now has actual Arabic/Paper/Huge/reduced and Hebrew/Ink/Huge/normal docked portrait/landscape proof, with all three compact geometry checks passing in both languages. Arabic nested sticker search releases the editor lease and reacquires it on close. Hebrew Home/resume, zero-overlap floating handwriting and settled/scrolled expanded Tools are visually inspected. Eighteen receipts and referenced PNG hashes match; the first wrong-keyboard-mode precondition, immediate cleanup read and transitional screenshots remain explicit exclusions, not passing tests. Original settings and English/Paper/Huge/normal Orb are restored; installed app/test hashes and all non-documentary build inputs still match.

No production source changed. T031's exact-final RTL/nested/observed-floating repetitions are closed, while its separate presentation criterion and original T009/T015/T018/T019/T023/integration gates remain open. Technical and settled editor Visual Runtime PASS are scoped; Motion remains FAIL, Artistic/Craft/Model and untested installed-PWA/WKWebView/Tauri runtime remain UNVERIFIED. Inline critic: FIX. Current exact-source helper scans are complete. See `output/android-103ms/keyboard-viewport-lease-20260909/FINAL_RTL_RUNTIME.md` and the main keyboard evidence packet. The following dated checkpoints retain historical status.

## Final-package verification and open raster boundary — 2026-09-09, 07:20 UTC

The final benchmark `bed4cf979b6115b662d92e6148d7501690502f12ee990699a726dce0a83d0e59` is independently matched on emulator-5560, package 2.1.2/39, PID 20480. All 96 recorded inputs match through six clean traces and offline attribution. Three final editor recordings contain zero strip detections among 7,298 decoded frames over 12 hide/refocus cycles; the full boards were inspected. Current English/Paper/Huge/normal docked-landscape/caret, Home/resume, portrait and fully settled Tools checks pass. Earlier ar/he/nested/floating observations retain their earlier artifact scope.

The uninterrupted final full CI exits 0: 10,334 tests pass, 23 skip and seven existing cases remain unfinished. Separate workspace/release contracts pass 374/650 cases. All seven final production-browser cases pass without retries/errors; normal and ar/he Huge screenshots were inspected. Oxlint and ratchet warnings remain explicit. This closes the former final-package/full-CI/browser gaps, not the original whole-goal acceptance.

Current native frame diagnostics remain FAIL. Three navigation runs have 15/13/21 app frames above 103 ms, maximum durations 193.218/221.607/226.107 ms. Three continuous Orb/theme runs have 4/7/9 display intervals above 103 ms, maxima 141.260/176.735/291.114 ms. Each capture has 12 measured real native actions and complete app/display-token matching, with no reported error/data-loss statistics. Planning idle gaps are not labeled freezes. Two trace-end incomplete WebView draws retain missing durations; native Up-to-next-frame diagnostics are not semantic readiness or final-presentation acceptance.

The separate complete Chromium Ink-to-Paper profile contains 221,330 events and no reported data loss. Its 113.958 ms longest draw includes 93.727 ms of sync-token waits; contained GPU command wall times are 91.818 ms for RenderWorker and 1.403 ms for WebGL. Layout peaks at 0.762 ms. Per-layer GPU ownership remains UNVERIFIED; numerical raster counters are not joined. The prior over-cap export stays excluded. The app returns to the same Paper/English/Huge/normal Orb without an editor/menu/lease/veil, and no production visual change is made from this attribution alone.

T030 remains implemented. T031 retains exact-final RTL/nested/floating repeats and separate presentation acceptance; T009/T015/T018/T019/T023 and all-branch main integration remain open. Web/Vite is browser-verified; installed PWA, actual WKWebView and packaged Tauri remain UNVERIFIED. The Android editor has scoped visual proof, while current frame pacing FAIL remains. Technical PASS is scoped to completed tests/build/browser; Artistic/Craft/Model remain UNVERIFIED and the full goal is not converged. Final prefix/frame/profiler scans have zero Snyk findings in their scopes; the two latest attribution helpers have a separate scan in progress. Full hashes, platform/domain rows, exclusions and restoration: `output/android-103ms/keyboard-viewport-lease-20260909/KEYBOARD_VIEWPORT_EVIDENCE.md`. This update is documentation-only after the captured snapshot. All following dated sections retain historical status.

## Native viewport, caret and docked landscape — 2026-09-09, 06:00 UTC

The leading 07:20 checkpoint's diagnostic scan is subsequently complete: exact-source suites `20260909T071916Z-9208` and `20260909T073444Z-19173` each exit 0 with no Snyk or secrets findings in their scopes. A same-APK A/B/A compositor-hint experiment does not improve theme-phase timing and is rejected, with its transient style removed. No additional production change is made; the full evidence packet records the retained failure, potential host-memory confound and unchanged app state. The historical implementation narrative resumes below.

T030 is implemented after T029's identical-app control, not from the standalone fixture alone. The existing SafeArea owner grants a scoped Android phone-editor lease, preserves/restores the exact window mode, rejects stale document/destroyed-owner requests and reserves actual overlap inside the full-height opaque editor. Native page-listener registration occurs after Bridge construction; the earlier load-time registration was demonstrably overwritten. Nested dialogs and other forms retain their legacy path. Production files are the SafeArea Java/patch, `useJournalKeyboardViewport.ts`, `journalCaretVisibility.ts`, `JournalEntryEditor.tsx` and nine Android-lease-scoped layout rules in `src/index.css`; canonical assets/materials, labels, storage, dependencies and native colors are unchanged by this correction.

The completed live native evidence passes 18 cases twice, using live/attached/measured per-test Activities rather than the superseded BeforeClass fixture. The native source and exact patch hashes remain `27ac9554…` and `d193743a…`. Host regressions include 29 lease/caret/compact cases and the exact pristine-to-patched Java replay. Actual RTL/Huge rotation exposed a hidden active line, then an ordinary docked landscape with only 122.667 CSS px above IME; the event-driven caret observer and conditional shared control row address these distinct failures. The measured row is 51.286 px, leaving 71.381 px for content, with the line and every original 48px-or-larger action visible. Portrait, floating/hidden keyboard and expanded Tools restore their original layout. Real ar/he, Paper/Ink, normal/reduced, resume and nested sticker-search checks are retained.

The recorded benchmark `com.zenflow.app` 2.1.2 / 39 has SHA-256 `4a81d66c4a0fc7e4c99de86ec0d79f4d5a66320eae42523dcda2070f3f82f5da`, with 95 build inputs stable and 234 web assets inventoried. Independent installed bytes, PID and path match around three full recordings. They complete 12 hide/refocus cycles, with 1,864 / 2,068 / 1,995 decoded frames and zero dark-strip signatures; all full-duration boards and crop alignment were inspected. No build/scanner/profiler/CDP ran during capture. This is scoped editor pixel evidence, not 103 ms final-presentation proof.

The first full CI run reports 10,332 passed, two failed, 23 skipped and seven existing unfinished cases. Its two failures were fixed without changing geometry: new warnings now use the existing `[Journal]` subsystem; the touch-target test parses the actual global control rule instead of the first Android rule and retains all size assertions. The same 54 cases and 14 patch/iOS/reflow neighbors pass; strict lint for the two follow-up files passes. The full uninterrupted rerun and final source-scoped security follow-up are in progress. This diagnostic-prefix-only source change postdates the captured APK; the old video hashes do not certify a future build.

At 06:09 UTC the exact two-file source follow-up passes one Snyk run with zero findings, and Gitleaks/TruffleHog report zero secrets. The second full CI attempt detects generated inventory drift after the warning change and is stopped with exit143; it is not a complete full-suite result. The unchanged generator refreshes 1,152 owners / 56 canonical exclusions / 803 candidate files with 100% coverage. Fresh doc-counts and constitution checks pass; the ten inventory regressions are being repeated before another full run.

The app was restored to Orb / English / Paper / Huge 1.5 / normal motion, without an editor, lease or diagnostic style. Original keyboard/font/rotation settings and original test APK `e23b3620…` were restored and verified; the current benchmark remains installed. No journal text, draft restoration/dismissal, record or mood was written. Isolated current-observer browser checks cover all eight scripts, newline/rotation and retained manual reading, not native text composition. The broad 71-file Snyk scan retains two diagnostic-only findings without suppression; the suite's raw exit1 is not zero-finding PASS.

Technical: PASS scoped, full rerun pending. Visual Runtime: PASS for the recorded editor cases, whole-goal UNVERIFIED. Artistic/Craft and Model: UNVERIFIED, no new human approval inferred. Motion: UNVERIFIED for current final presentation; earlier over-budget runs retain their dated FAIL. Plan: PASS for this bounded root-cause direction, not convergence of the full goal. Inline critic: FIX.

T031 and T009/T015/T018/T019/T023 remain open. T028's animated-padding candidate is superseded/rejected, not a pending implementation to restore. Final build/browser/frame evidence and the original all-branch main integration remain outstanding. Web/PWA uses no native lease; iOS/desktop paths are unchanged and statically checked where applicable, but installed PWA/WKWebView/Tauri runtime and other devices/providers remain UNVERIFIED. Detailed hashes, command receipts, exclusions, security triage, rollback and matrix: `output/android-103ms/keyboard-viewport-lease-20260909/KEYBOARD_VIEWPORT_EVIDENCE.md`. Earlier dated sections below are historical.

## Isolated provider control — 2026-09-08

The user-authorized fresh-AVD control is complete, not the whole fix. The same static APK and legacy native inset handling reproduce a dark strip on WebView133 and Chromium snapshot155 with ordinary Gboard: four episodes in each matched capture. The first155 recording contains handwriting onboarding and is excluded despite its zero detector count. Fresh installed hashes, complete recordings and inspected frames support the bounded negative result; the single matched pair does not establish relative performance or103ms acceptance.

The real df17ccd6 benchmark on155 mounts the Orb error screen and logs WebGL1 blocklisted. The plugin's140+ inset branch therefore has no canonical-Orb visual acceptance here. No blocklist bypass, graphics downgrade or new production native candidate is adopted. The disposable AVD is stopped; original qemu13633/app24378, WebView133, APK hash and three OS settings are freshly preserved. The original current route is Diary, not the earlier Settings checkpoint; no cause or full appearance/storage equality is claimed and no forced restoration is made.

Three exact diagnostic sources pass the required Snyk CLI/secret checks. Fresh check:all exits 0 with ten existing palette warnings. The first full ci:preflight exits 1 on one stale generated-inventory test. The unchanged generator refreshes the inventory to 1,150 owners / 56 canonical-Orb exclusions / 801 candidate files, and the identical ten inventory tests pass. The second unchanged full preflight completes with exit 0, observed on 2026-09-09 at 00:56:48 UTC: 864 files / 10,303 tests passed, one file / 23 tests skipped and seven existing tests unfinished. Coverage-evidence, production-web build and every downstream gate pass; ratchet reports 9.2 / 10, zero violations and 70 warnings. All 82 source-path hashes match the pre-coverage snapshot before reporting edits. This is local technical proof, not general runtime acceptance. The preceding two-registry inventory identifies 15 open Dependabot PRs, three with failed checks, and preserved dirty legacy/main state; no branch integration occurred. T009/T015/T018/T019/T023/T028, the complete glitch fix and requested all-branch main integration remain open. Full captures, exclusions, hashes, restoration boundary and inline critic: `output/android-103ms/webview-control-20260908.6pkevy/WEBVIEW_CONTROL_EVIDENCE.md`; the new uninterrupted-CI receipt and integration boundaries: `CI_AND_BRANCH_EVIDENCE.md` in the same evidence directory. The following sections retain their dated scope.

## IME rendering boundary — 2026-09-08, 23:05 UTC

No further production workaround is adopted. The animated-padding and fixed-surface candidates are rejected by their actual recordings, despite scoped native test successes. On the restored df17ccd6 benchmark, hiding all descendants of the fully covered Diary background reduces observed compositor layers but leaves four dark-strip episodes. A separate static diagnostic APK on the same API36/WebView133 emulator reproduces the strip without React, application JavaScript, app data or WebGL, both with and without its two chrome blur layers. This rules out those features as necessary conditions for the minimal symptom; it does not establish a provider bug, relative performance improvement or a complete app fix.

A separate native trace contains a 2,223.601 ms WebView draw and a 2,202.772 ms main-thread postAndWait. Chromium's GPU thread runs for 1,741.036 ms during that draw. These are scheduling/slice measurements, not hardware GPU time or input-to-photon proof; the trace covers only seven of the eight commanded IME animations. Both static-fixture captures complete eight native actions and fully decode; inspected original frames retain the defect. Earlier main-PID-only log filters do not exclude renderer-process errors.

The disposable package is removed, its evidence retained, the original benchmark/test APK hashes rechecked, 18 appearance controls and the public root restored exactly, the handwriting override deleted, and owned forwards removed. Thirteen exact diagnostic sources pass Snyk with zero findings and the stated local secret checks; an incorrectly scoped aborted scanner attempt is excluded and documented. No other session, provider or production source was changed. Technical PASS is diagnostic-only; Visual Runtime/Motion FAIL; Artistic/Craft/Model/Plan UNVERIFIED for whole-goal acceptance. T009/T015/T018/T019/T023/T028 remain open; no commit, push or all-branch main integration occurred. Full artifacts, limitations, platform matrix and inline critic: `output/android-103ms/gpu-cpu-attribution-20260908.2GdUKi/IME_MINIMAL_REPRO_EVIDENCE.md`. The native viewport/rendering direction requires discussion before another production fix; the earlier approved work is not discarded or restarted.

## Current native Back repair — 2026-09-08, 19:50 UTC

T027 now closes the reproduced queued-owner teardown crash. The real-main-queue Android test runs three controls successfully before the pre-fix fourth case crashes at AndroidBackPlugin:57; the identical test source and test APK pass all four cases after moving callback validation inside the main-thread update, before ownership state changes. Thirteen native neighbor checks, ten separate Gradle unit tests and34 JavaScript Back checks pass. The two exact Java files pass scoped Snyk with zero findings; the local quick-suite raw reports are retained.

The installed benchmark is2.1.2/39, SHA-256 `df17ccd6e5e1f79459d2c6733b4a3c29ca36525548406292d1d40565531c7db3`. Independent post-QA reads match; all81 source-path hashes remain unchanged through closeout, and234 web assets match the preceding SafeArea candidate. Actual Back closes menu/editor, Diary survives three Home/resume cycles, and settled root edge-Back delegates to Android. Original test APK and keyboard preferences are restored; owned forwards are absent. Evidence, excluded early gesture observation, source hashes, scope and rollback: `output/android-103ms/gpu-cpu-attribution-20260908.2GdUKi/ANDROID_BACK_LIFECYCLE_EVIDENCE.md` and `back-race-closeout.json`.

The light native bars under OLED are confirmed current fixed-contrast behavior in themeStore and its existing test, tracing to commit51449f60d; the earlier visual observation is not treated as a proven new regression or permission to redesign them. Docked Arabic/Huge/reduced editor painting remains FAIL. After root recreation, one later native accessibility snapshot contains only the WebView despite visible native pixels and mounted DOM; accessibility-tree completeness remains UNVERIFIED. Technical PASS is limited to the repaired behavior, Visual Runtime/Motion FAIL overall, Artistic/Craft/Model UNVERIFIED, and inline critic FIX. T009/T015/T018/T019/T023 remain open. No commit, push, main integration or release occurred. Following sections retain their dated evidence, not current unmodified-Back or old-APK claims.

## Current root-cause continuation — 2026-09-08, 19:13 UTC

The resumed goal remains active. The Diary security-loading foreground is now explicit and navigable;76 component tests,117 boundary and60 security tests pass, along with the recorded source/build/scanner checks. Native route recording includes two real loading states with header/menu/status and no hidden-input-only foreground. Full evidence and exclusions: `output/android-103ms/gpu-cpu-attribution-20260908.2GdUKi/SECURITY_LOADING_EVIDENCE.md`.

The subsequent SafeArea repair fixes a separate reproduced floating-Gboard defect: visible IME with zero bottom overlap no longer removes63 px navigation-bar padding. T024–T026 are complete within their written repair/verification scope. Four expected RED failures become seven identical native GREEN tests, plus one passing backdrop regression; patch replay and a two-Java-file Snyk scan pass. The exact installed benchmark is2.1.2/39, SHA-256 `6a4b161dea3d8bfbeda7321620e98b9bc432006497095f62ec3ef038ac23d853`;234 web assets are byte-identical to the preceding loading candidate. Three English floating focus/Back cycles and Arabic RTL/Huge/reduced-motion floating inspection retain fully visible controls. Public appearance preferences, OS keyboard settings and the original test APK are restored, and owned forwards are absent.

Whole-goal acceptance remains open. Docked Arabic/Huge/reduced native screenshots still omit or clip header controls, even with correct viewport/Tools bounds. The first native RED attempt also exposed an actual queued `AndroidBackPlugin.setState` teardown crash; its log is retained and no fix is claimed yet. Frame/presentation103ms failures, OLED bars and other earlier gaps remain unresolved. T009/T015/T018/T019/T023 remain open; no phone, other-native-platform, human artistic, release or main-integration acceptance follows from the local fixes. Technical PASS is scoped; Visual Runtime/Motion FAIL overall; Artistic/Craft/Model UNVERIFIED; inline critic FIX. Evidence: `output/android-103ms/gpu-cpu-attribution-20260908.2GdUKi/SAFE_AREA_KEYBOARD_EVIDENCE.md` and `safe-area-closeout.json`. Dated sections below are historical, including their earlier APK and goal-status descriptions.

## Approved Diary Orb background — 2026-09-08

The owner-approved V2 Diary background is implemented SOLO in the same lane. DiaryPage composes the existing Orb day/night scene and passes it through an optional JournalModule presentation slot. The old mountain wallpaper is absent on this page; default/V1 callers and the separate mobile editor retain their existing treatment. No canonical scene, shader, asset, data path, native code, dependency or agent rule was changed by this extension.

T020–T022 are complete within their written technical scope: 10 expected pre-implementation RED assertions; the same 78 tests GREEN; 181 journal/security/scene and 105 navigation tests GREEN; separate typecheck, check:all, canonical/no-template/context, source/bundle PDI and scoped Snyk checks pass. Snyk directly covers all four changed TSX files with zero findings. The exact benchmark APK is 2.1.2 / 39, SHA-256 `fde9ee6783da2b4c43938850429eaf234f826ec91f44cc37d72d1e4e1af36eba`, independently verified installed. All 76 source-path hashes and 15 build inputs match across packaging; later changes are documentation closeout only.

Native settled Paper/Ink and Arabic Paper / Hebrew OLED reduced-motion Huge layouts show one complete background and readable foreground. Eight production-equivalent browser screenshots cover phone/wide, day/night/OLED, ar/he and reduced motion; the manual run has zero console messages and 104 HTTP200 responses. Physical-phone, installed PWA, iOS and packaged Tauri proof remain UNVERIFIED; absent phone hardware does not block emulator work.

Acceptance remains open. New-candidate clean application FrameTimeline runs contain 96/74/71/126 frames over 103 ms, maxima 551.510/824.332/838.916/586.246 ms. The newly reinstalled old F3 control also fails (62 frames over budget, maximum 584.991 ms), so the new backdrop is not established as the sole cause. Hiding it temporarily is diagnostic only and still fails; that style was removed, the new APK restored and no quality reduction adopted. These are frame durations, not input-to-final-presentation latency.

The native IME/gesture-bar overlap persists; OLED native bars remain light. Reduced-motion Home/resume retains the page, while the immediate normal-motion resume screenshot is blank and the later settled screenshot is complete despite unchanged DOM geometry. Seamless presentation is not accepted. Original settings were restored, no records were written and the owned WebView forward was removed. Technical: PASS scoped; Visual Runtime/Motion: FAIL; Artistic/Craft/Model: UNVERIFIED; Plan: PASS for this bounded extension, not whole-goal completion. Inline visual-integrity-critic verdict is FIX.

T023 and original T009/T015/T018/T019 remain open. No commit, push, main merge, public deployment or new goal was produced. The goal API still reports blocked and provides no resume operation; this code work proceeded without fabricating automatic resumption. Full evidence, platform/domain matrix, exclusions, rejected causal claims, screenshots and cleanup are in `output/android-103ms/diary-orb-background-20260908.fmRc1u/DIARY_ORB_BACKGROUND_EVIDENCE.md`. Earlier dated sections below remain historical.

## Offline recovery after XProtect stop — 2026-09-07 UTC, 23:15

Session `01a0760c-2c7c-73f2-b799-d5ecfb22ac01` resumed SOLO in the same locked lane after the owner reported XProtect's 21:54:28 UTC rules-engine denial and emulator termination. The emulator and interrupted CDP chain were not restarted. The exact denied script/URL and causal relationship remain UNVERIFIED; reviewed local diagnostic source is not proof that replay is safe.

All 60 saved dirty file hashes matched before this documentation update. The three navigation suites plus Android evidence suite freshly pass 164 tests. Offline analysis of the retained Settings trace identifies a 46.288 ms RenderThread sync-token wait (69.791–116.079 ms after pointerup), overlapping 40 Chromium GPU-thread tasks and 32 raster-end operations. These are trace wall-time intervals, not hardware GPU time, final presentation, or a proven fix. Negative controls reject missing input, data-loss metadata and missing thread identity.

No production behavior or accepted visuals changed. Runtime recovery is incomplete: reduced motion was last changed from true to false for the interrupted diagnostic; restored preferences, final installed hash and owned-forward cleanup after the incident are UNVERIFIED. The earlier restoration paragraphs retain their dated scope. T009/T015/T018/T019 remain open. Current safe-continuation evidence: `output/android-103ms/offline-recovery-20260907/REVIEW.md` and `trace-attribution.json`. A temporary outside-repository scan-directory creation and the security-suite invocation were denied by guards; neither was retried through another executor.

## Navigation feedback handoff — 2026-09-07 UTC, 16:24

The requested continuation now includes a concrete Android navigation fix: `useNavigationV2.ts` clears outgoing feedback in the destination commit, preserving destination Suspense loading, retained drawer ordering/cancellation and non-Android behavior. The old 320 ms cosmetic hold no longer keeps the Android indicator and its blur surface after handoff. Six new hook cases and stronger destination-loading assertions pass in the actual three source suites: 105 tests. Strict lint, benchmark sync/Gradle and scoped source security checks pass; Snyk reports one run and no findings.

The final built and independently observed installed APK is `50c3f6bfb2a66430aab4b8268d0db849060692f467cbbdf8d5a98e08e30f0aed`, package 2.1.2 / 39. All 21 selected inputs match across packaging. On Arabic/RTL Huge text with reduced motion, 15 native route selections across three passes reach a settled route/pending/fallback DOM state in 38.3–95.8 ms. These observations do not include Orb visual readiness or final presentation. With normal motion, five native selections still take 168.6–211.2 ms; drawer removal alone takes 111.4–139.4 ms. The 103 ms normal-motion gate therefore remains FAIL.

Ordinary Gboard still clips Back/save controls on the final APK. Missing Orb/foreground after repeated route changes also reproduces on the independently reinstalled old `9ac94b0e…` APK, despite ready DOM/canvas state. This rules out the navigation patch as a necessary condition for that defect; it does not establish full visual parity or its cause. The temporary scroll-layer and Vulkan-backend diagnostics were rejected, with original settings restored. No renderer quality reduction or native workaround was adopted.

All seven compared public preferences match their starting values. The app ends on Orb selection; original keyboard settings and Skia OpenGL are restored, and the owned CDP forward/temporary observer are removed. Unrelated initial dirty work is preserved. Current evidence and the final CI/browser results are linked from `output/android-103ms/software-convergence-20260907/REVIEW.md`. T009/T015/T018/T019 remain open for the actual visual/frame, normal-motion/presentation and physical-phone requirements; a new report does not close them. All older packets below retain their dated scope.

## Daylight draw-dispatch applied — 2026-09-07 UTC

The owner explicitly approved both proposed paths. The reviewed `useAndroidDayLargeEffects.ts` patch is applied; its component test also includes a typed-lint correction to the resize callback. The generated motion inventory was refreshed after Android synchronization with unchanged discovery counts and 100% coverage.

VERIFIED: the original duplicate-startup regression changed from RED to GREEN; 111 focused checks, 10,240 full-suite tests and seven browser navigation cases passed. The full suite retains 23 skipped and seven pre-existing todo cases. Chrome performance smoke completed 14 route/profile checks with no console or network errors. The exact built and independently observed installed APK is `9ac94b0edec4a221b9ee916792694f0615f9442e7130061af64bec0868f7c015`. Production code and its test passed scoped Snyk with no findings.

Two native Light returns now submit one full draw set per browser timeline tick, compared with three on the prior `4e496f6e…` APK. Four fixed daylight phases (0, 1,000, 5,000 and 12,500 ms) have identical PNG hashes and zero different RGBA pixels at 1082 × 2202. This proves scoped buffer parity and dispatch coalescing, not complete transition or presentation acceptance.

Three separate 90-second frame runs retain 10/2/2 display gaps above 103 ms, with maxima 480.116125/164.366167/133.031001 ms. Host CI ran concurrently; direct baseline performance attribution is UNVERIFIED. The independent native video completed eight actions and 13 checkpoints. Its first day-menu checkpoint lacks the miniature orb; later night/restored-day frames and a fresh warmed opening show it. Causality and reproducibility remain UNVERIFIED, and broad visual parity is not accepted. The earlier IME failure remains unresolved.

Observed public preferences were restored exactly. No physical phone is connected. T009/T015/T018/T019 remain open: 15/19 main IDs checked. Evidence is in `output/android-103ms/day-render-applied-20260907/`, especially `dispatch-comparison.json`, `pixel-comparison.json`, `frame-summary.json`, `visual-review.json` and `ci-preflight-after-inventory.log`. Earlier dated packets below remain historical evidence; the proposal is no longer awaiting permission.

## Frame attribution and IME synchronization — 2026-09-07 UTC

Three saved current-APK traces associate one long display gap per run with Light theme activation. During those draw calls, RenderThread sleeps for 82.38–96.91 ms; Chrome_InProcGp wakes its longer sleeps and is CPU-running for 86.64–115.69 ms. This identifies a rendering dependency, not the underlying Chromium operation or hardware GPU duration. Other gaps occur outside immediate input windows. Clock alignment remains approximate.

A temporary native postVisualStateCallback viewport experiment on the unchanged `4e496f6e…` APK still leaves the Back control blank after ordinary IME resize. It is rejected. Two initial captures are excluded because the handwriting tutorial intercepted the first tap. The three subsequent native captures were inspected; the aligned candidate and restored CSS have identical geometry. All 57 pre-existing dirty files remained unchanged during diagnosis. Public preferences and keyboard settings were restored, and the test package plus ten hash-verified device captures were removed.

No production fix or acceptance checkmark follows. T009/T015/T018/T019 remain open: 15/19 main IDs checked. Physical-phone and full visual/motion acceptance remain UNVERIFIED. Evidence: `output/android-103ms/frame-attribution-20260907/analysis.json` and `output/android-103ms/ime-frame-sync-20260907/receipt.json`.

## Current APK frame and video verification — 2026-09-07 UTC, 03:19

The same installed `4e496f6e1fb9c9ed748e230010e8008a0a6867c39f1aa2377c07301fe9027b2d` APK now has three separate warmed 90-second emulator traces in English/LTR, Huge text (1.5), normal motion and 60 Hz. They link 5,309 / 5,346 / 5,309 frames with no missing display tokens or reported trace errors/data loss. All three exceed the display-gap budget: 1 / 2 / 3 gaps above 103 ms, maxima 116.865834 / 109.980583 / 159.195583 ms. Each native scenario completes eight actions; this does not establish interaction latency. A separate 54.496867-second native video fully decodes to 2,268 frames; six inspected settled checkpoints show Orb/menu controls in both themes. Full transition parity and phone acceptance remain UNVERIFIED. Four independent installation pairs validate; all 18 build inputs match. Arabic/RTL, Huge, reduced motion and auto theme were restored; owned trace copies/forwards were removed after verification. No production code changed. T009/T015/T018/T019 remain open, 15/19 checked. Evidence: `output/android-103ms/current-apk-frames-20260907/receipt.json` and `summary.json`.

## Theme phase diagnosis — 2026-09-07 UTC, 02:55

The unchanged installed APK `4e496f6e1fb9c9ed748e230010e8008a0a6867c39f1aa2377c07301fe9027b2d` was exercised with 24 native theme activations under Arabic/RTL Huge text. Fourteen minimally observed baseline activations take 118.6–297.0 ms from DOM activation to veil cleanup; all exceed 103 ms. These are event/phase measurements, not input-to-photon evidence. Separate trace and animation-state reads identify pending release startup and repeated native Back visibility/layout work, without proving the whole cause. Explicit release-clock startup still exceeds 103 ms in four of six attempts and was rejected. No production change follows. Public preferences were restored exactly, temporary observers and the owned forward removed, and all 18 build-input hashes verified unchanged. T009/T015/T018/T019 remain open; 15/19 main IDs are checked. IME visual failure and physical-phone/presentation acceptance remain unresolved. Evidence: `output/android-103ms/theme-phase-20260907/receipt.json` and `analysis.json` in that directory.

## CI repair and native continuation — 2026-09-07 UTC, 02:25

The same locked lane remains at HEAD `939daa7e117331ef75201cff3a2c74d314861be2`, with an empty index and unrelated dirty work preserved. The existing DrawerV2 primary navigation definitions and JSX were moved to DrawerV2Destinations without changing their normalized content, visual classes or callbacks and without adding a DOM wrapper. DrawerV2 retains focus, portal, state, exit, header/footer and settings ownership. The source line count falls from 504 to 400; the extracted component has 149 lines. The measured architecture CSS count was corrected from 7,717 to 7,804 before extraction. The unchanged inventory generator now records 1,149 owners, 56 orb exclusions and complete discovery.

The same 99 focused tests pass before and after extraction. Seven production-browser cases pass without retries or skips, and normal/ar/he Huge drawer screenshots were inspected. Source-only security scanning covers the two exact components: Snyk reports one run and zero findings; the other executed quick-profile scanners exit 0. Full uninterrupted `npm run ci:preflight` exits 0, including 374 workspace checks, 650 release checks, full coverage, build, integrity, audio, RAG, completion, sync, schema and the ratchet. Full coverage records 862 files and 10,240 tests passed, one file/23 tests skipped and seven existing unfinished tests. The unchanged ratchet reports three oversized components against three, score 9.2 against 9.2, zero violations and 70 warnings. The earlier stale-build and ratchet failures are retained as before evidence.

The ordinary benchmark build/sync and Gradle commands completed successfully. All 18 selected build inputs and the full recorded source snapshot are identical before/after packaging. The new APK SHA-256 is `4e496f6e1fb9c9ed748e230010e8008a0a6867c39f1aa2377c07301fe9027b2d`, package `com.zenflow.app`, version 2.1.2 / 39. Independent installed-byte reads match after replacement and after diagnostics on emulator-5560 with WebView 133.0.6943.137. Native Arabic/RTL Huge/reduced drawer controls fit and exceed 44 CSS px. Actual taps reach Habits, Diary, Planning, Settings and Orb and dismiss the drawer; immediate route observations contain pending/loading markers. This proves scoped destination activation, not completed readiness or native transition timing. Earlier APK frame/video evidence does not transfer to these new bytes.

The IME investigation first established that ADJUST_NOTHING does not prevent resize: the existing SafeArea listener owns decor padding. An initial fixed-surface harness crashed before a measured layout; it is retained as a harness failure. Early externally timed restoration screenshots captured the launcher after instrumentation ended and are invalid comparison evidence. The corrected automatic collector captures eight native screenshots with IME visible before and after each image. Across two cycles on the earlier APK, fixed full-height WebView plus matched CSS height displays controls; restored native height with the same CSS again clips foreground. Removing the CSS then restores foreground at ordinary dimensions. This rules out a final fix based only on the promising fixed-surface image.

On the newly installed APK, a second eight-image automatic run preserves ordinary native bounds. Native invalidation and offscreen pre-rasterization do not repair clipping. Writing the measured viewport height into the existing CSS variable restores foreground with identical recorded textarea bounds and scroll position. A separate prototype that mirrors viewport resize into that variable still produces clipped labels/buttons after actual keyboard opening, so it is rejected as a production remedy. The exact rendering root cause remains unresolved. Both final native probe sources have one Snyk run with zero findings. No IME production source or dependency change was made.

Temporary DOM styles/listeners, instrumentation and the owned CDP forward were removed. Thirty-two diagnostic device files were hash-verified against retained local copies before removal. Original keyboard preferences are restored; the app ends on the Orb select surface with Arabic/RTL, Huge text, reduced motion, paper theme and no experiment styles. No note or mood was saved. The CI technical subgate is PASS; ordinary IME Visual Runtime is FAIL. Current-candidate repeated frame/video, 103 ms final presentation, phone, human craft/model and unchecked installed PWA/iOS/Desktop runtime remain UNVERIFIED; prior measured timing failures retain their dated scope. T009/T015/T018/T019 remain open and the main count is 15 of 19. No commit, push or release occurred. Current receipts: `output/android-103ms/drawer-structure-20260907/receipt.json` and `output/android-103ms/ime-surface-20260907/receipt.json`. The following sections are historical.

Final documentation verification also detects the generated V2 coexistence count changing from 56 to 57 after extraction. `npm run doc-counts:update` refreshes that block through the existing generator; the preceding failure is preserved. An initial completion-check invocation used a nonexistent package-script name, so it supplies no proof; the actual `check:task-completion` command is recorded separately. These documentation/tool-invocation corrections do not change APK source inputs or the earlier successful full CI result.

## Ordinary IME diagnosis — 2026-09-07 UTC

The approved locked lane remains at HEAD `939daa7e117331ef75201cff3a2c74d314861be2`. This continuation changes evidence documentation only. The installed application remains package `com.zenflow.app`, version 2.1.2 / 39, SHA-256 `a68fa7b01bacd4949fcfc9a06c0ac70b45405f6f7b70a75579fca8df3aa6cc5a`, independently read again after restoration.

Ordinary Arabic Gboard is now exercised through actual native Next and empty-note controls after temporarily disabling handwriting onboarding. With Arabic/RTL, Huge text (1.5) and reduced motion, native WebView bounds change from `[0,136][1080,2339]` to `[0,136][1080,1520]`, and the CSS viewport from 839 to 527 px. Settled framebuffer captures and an actual Emulator-window recording show missing or rectangularly clipped foreground controls and textarea edges. The corresponding CDP WebView surface capture contains the complete foreground. This establishes an ordinary IME visual FAIL and a capture-path discrepancy; it does not establish the precise compositing root cause or a physical-device result. No note was entered or mood saved.

The same failure persists with the software GPU backend and with a temporary native hardware layer. A test-only instrumentation APK completed its diagnostic phases and was uninstalled; its completion is not a regression-test GREEN. Textarea-only blur removal, scroll-layer promotion, root render-pass and paint-containment probes do not establish a fix. Globally disabling backdrop filters restores foreground but removes visible canonical effects and was rejected. Early offscreen-blur observations followed that global redraw and are confounded. A later isolated offscreen-only experiment begins from a freshly reloaded, matched Huge/reduced baseline and still fails. All DOM styles, probe attributes and owned CDP forwards were removed.

A separate `-read-only` emulator tested an official Chromium snapshot WebView, version 155.0.8044.0, after archive integrity and APK signature verification. It cannot complete the Orb flow: the native log reports WebGL blocklisting and the app displays its real unavailable/error surface. Therefore the old/new-engine comparison is UNVERIFIED, not a fix. Switching the disposable provider back reset some preferences; matched Huge/reduced conditions were restored through actual Settings controls before the final containment and offscreen tests. The disposable instance was stopped. All seven original AVD data/configuration files match their pre-comparison SHA-256 values. Original emulator-5560 was restarted with its host GPU, WebView 133.0.6943.137, original keyboard settings, Arabic/RTL, Huge text and reduced motion. The diagnostic package and snapshot provider are absent from that original instance.

No production source, dependency or application APK changed. The source-only security scan of the native diagnostic reports one Snyk run with zero findings; its report is `/Users/yehor/.codex/security/reports/20260907T001308Z-48435`. Prior full-suite/build/browser results retain their original dates and scope. The ratchet and finite-interaction timing failures remain, ordinary IME visual acceptance is now FAIL, and physical-phone, continuous human visual review and unresolved platform coverage remain UNVERIFIED. T009/T015/T018/T019 stay open; the main-task count remains 15 of 19. Detailed experiments, rejected/confounded evidence, restoration checks and final documentation verification are recorded under `output/android-103ms/notify-completion-20260906/`, summarized in `receipt.json`. The following sections are historical.

## Current verification continuation — 2026-09-06

The owner resumed the approved plan and the exact release-discovery command edit was accepted. `package.json` adds only the existing `--exclude 'output/**'` option to `test:release-contracts`; all 39 mandatory test-file arguments, the workspace prefix and dependency declarations are unchanged. The same process-level regression goes from two RED cases to all four GREEN, including rejection of a failing canonical contract. The real command passes 374 workspace and 650 release checks.

Full coverage passes 862 files and 10,240 tests, with one skipped file, 23 skipped tests and seven existing unfinished tests. Coverage evidence, lint, types, i18n, production build, source/bundle data integrity, artifact integrity, CSS, size, canonical orbs and policy checks pass. CI initially stops when the unchanged audio guard finds retained negative-test fixture copies under output. Four exact three-file test directories were moved to system temporary storage with verified before/after hashes and a retained manifest; no audio rule was excluded or changed. Audio, Hyperfocus, RAG, completion, sync and schema checks subsequently pass. The final ratchet remains FAIL: four oversized components versus a floor of three, and score 9.0 versus 9.2. A read-only scan of HEAD with the identical validator confirms the same four oversized components already existed. No unrelated component refactor or floor change was made. The original CI exit remains 1; separately passing later stages do not turn it into an uninterrupted successful CI run.

All seven production-browser cases pass without retries. First/warm visits cover all five destinations in both motion modes; ready observations exclude real loading/error surfaces. The normal, Arabic Huge and Hebrew Huge attached screenshots were inspected, with controls fitting the viewport and meeting the tested 44 px bounds. This closes T008 within its written browser scope. These diagnostics are not native timing proof: their maximum DOM-ready observations are 1270.3/962 ms under normal/reduced motion, and the browser run overlapped full-suite work.

The installed APK remains `a68fa7b01bacd4949fcfc9a06c0ac70b45405f6f7b70a75579fca8df3aa6cc5a`, package 2.1.2 / 39, on emulator-5560. Two new independent 90-second traces each match 5,385 display frames, with 5/6 missed deadlines, no gaps over 103 ms and maxima of 66.763583/66.343750 ms. Both journeys complete eight native actions and thirteen checkpoints; before/after installed hashes and PID 28678 match, no CDP forward or video recording is active during tracing, and trace statistics have no nonzero error/loss rows. The earlier same-APK trace still has seven gaps over 103 ms, maximum 258.622958 ms. These results measure display intervals, not total transition latency.

Six native theme taps with minimal DOM/event observation take 154–190.9 ms from activation to veil removal (p50 161.5 ms). A temporary exact-blur-retention experiment takes 158.6–171 ms and does not resolve the budget; the temporary style and observers were removed. No computed-style sampling, rAF observer or Chrome tracing ran in those samples. The original trace's seven long gaps include events before the first action and during idle; time proximity to an action is not causality. Ordinary keyboard resize remains UNVERIFIED after Android handwriting onboarding; the temporary system IME preference was restored. No mood record was saved.

Technical acceptance remains FAIL on the ratchet; Android finite-interaction timing remains FAIL. Native visual continuity, artistic/human acceptance, full input attribution, ordinary IME and physical-phone acceptance remain UNVERIFIED. T009, T015, T018 and T019 stay open; 15 of 19 main tasks are checked. No new APK, dependency, commit, push or release was produced in this continuation. Current receipts are under `output/android-103ms/finish-plan-20260906/`, summarized in `receipt.json`. All following dated sections retain historical evidence only.

## Current continuation — 2026-09-06 17:42 UTC

The exact installed `a68fa7b0...` candidate now has separate complete video and frame captures. The 210.016667-second window video contains 10,564 decoded frames, 24 actions and 36 checkpoints; independent APK reads agree, PID is stable and the captured PID-scoped log has no matched fatal/ANR/context-loss/tile signatures. Back/theme frame boards still show loading or covering intervals. Full continuous human visual review is UNVERIFIED.

The separate 90-second trace contains 5,204 matched frames, 84 missed deadlines (1.614143%), 57.844108 presented fps and seven presentation gaps above 103 ms, maximum 258.622958 ms. No nonzero trace error/data-loss rows were found. The 103 ms target remains FAIL. Instrumented main-thread attribution includes style/React work and observer overhead; it is not proof of one production root cause.

Settled English, Hebrew and Arabic native drawer screenshots and geometry were inspected with Huge text (1.5). All five destination controls fit the viewport and exceed 44 CSS px. The immediate first Arabic probe caught the opening panel offscreen and is retained separately. TalkBack and system 200 percent font coverage remain UNVERIFIED. Empty textarea focus opened Android handwriting onboarding; Back restored height from 863 to 839 CSS px. The immediate post-dismiss screenshot has unpainted regions, while the later settled screenshot restores the controls. Ordinary soft-keyboard resize and recovery timing are UNVERIFIED. No mood was saved. Arabic, Huge text, reduced motion, system theme and system font scale 1.0 were restored; only the task-owned CDP forward was removed.

Full tests ran with 10,237 passed, three failed, 23 skipped and seven existing unfinished tests. The stale generated motion inventory was refreshed by the unchanged generator, retaining 1,148 owner IDs and 100 percent discovery; all ten inventory tests subsequently pass. Two release-command archive-discovery REDs remain pending the owner decision. No full-suite success follows from the focused rerun. Technical and motion gates remain FAIL, artistic/phone and unchecked PWA/iOS/Desktop runtime gates UNVERIFIED. T008, T009, T015, T018 and T019 stay open. Receipt: `output/android-103ms/resume-20260906/continuation-native-qa-receipt.json`. Earlier sections retain historical evidence only.

## Current continuation — 2026-09-06 17:12 UTC

This update supersedes historical blocked-build and unmodified-runtime statements below. The normal Gradle/install path now works in the same locked lane. Native backdrop changes preserve current decor padding; a real Android regression went from RED to GREEN and three Home/resume cycles preserve viewport height, scroll position and control bounds. The exact native-insets APK, source and scanner evidence are in `output/android-103ms/resume-20260906/native-insets-receipt.json`.

Three morning 90-second traces on that APK show no display gaps above 103 ms, and a valid 210.015-second window recording contains 10,685 decoded frames. A later same-APK control has three gaps above 103 ms. The selector-only theme candidate has nine. These are dated emulator observations, not controlled physical-phone acceptance or proof of a CSS cause; the slower trace also has delays before the first theme tap.

The real browser reproduced an atomic-palette selector disabling the veil's own opacity transition. Its narrow exception passes the regression. A separate browser RED measures 276 ms of finite phases; Android-only 16/32 ms candidates reserve processing headroom without changing colors, opacity or easing. The installed candidate hash is `a68fa7b01bacd4949fcfc9a06c0ac70b45405f6f7b70a75579fca8df3aa6cc5a`. Instrumented WebView activation-to-DOM-removal is still 370.9/223.7 ms, so 103 ms is not achieved. The latest seven-case browser run has six passes and one drawer-removal timeout; three isolated normal-navigation repetitions subsequently pass. The first failure and uncertain cause are retained.

The Stop checker now excludes only exact technical/negated outcomes and uses Unicode word boundaries, preventing “плавности” from matching “плавно”. All packet extraction and validation code remains byte-identical. Hook and neighboring suites pass 466 tests; scoped Snyk has one run and zero findings with source hashes rechecked. The earlier rejected release command change separately passes all four isolated proposal tests, including rejection of a failing canonical contract. Live package.json remains unchanged pending the requested owner decision.

Fresh check:all, types and source/bundle PDI pass within their scope; no fresh complete-tree test success is claimed. Android remains the primary runtime target. Shared selector behavior affects Web/PWA/iOS/Desktop; native installed PWA, WKWebView and Tauri acceptance is UNVERIFIED. Physical-phone and human visual acceptance are UNVERIFIED. T008, T009, T015, T018 and T019 remain open, with no commit/push/deployment. Snapshot: `output/android-103ms/resume-20260906/continuation-1712-receipt.json`.

## Retained rendering attribution and Back characterization — 2026-09-06 UTC

Saved reports and exact video stills were re-read without new device commands or trace-processor queries. Fresh hashes match the three retained traces and the original full-route window video. Only run one has per-phase attribution. All three saved gfxinfo row windows begin more than 32 seconds after their journey ends, retain just 120 frames and have zero DisplayPresentTime values; they cannot supply missing interaction presentation measurements. The retained Perfetto motion failures remain unchanged.

The loading surface after Back maps to OrbPage's new visual attempt, keyed ValenceOrb lifecycle and fresh-readiness-plus-rAF reveal. The forced full-size hero branch already uses a zero start timer, so the generic 180 ms upgrade delay is not an established cause. A new characterization protects the complete Next/Android Back/stale-callback/new-callback path and verifies no mood save. The suite passed 44 tests before and 45 after the addition; typecheck, standard lint, canonical-orb guard and PDI diff passed. Production runtime and canonical visuals were not edited. The test controls the renderer callback and therefore does not measure GPU or native timing.

The local viewer at `output/android-103ms/attribution-recheck-20260906/viewer.html` was inspected through Chrome MCP at 500/1200 px, with original-video metadata, seek and muted 1x playback checked. `receipt.json`, raw logs, source/trace hashes and `saved-report-coverage.json` in that directory distinguish measured facts from source-level inference. Prior refusals and pending RED cases remain; no new APK, installed-byte identity, phone result, human motion approval, commit or push was produced. T008, T009, T015, T018 and T019 remain open.

## Tooling continuation and preserved RED — 2026-09-06 UTC

The owner's request to perform the necessary work led to a bounded release-contract discovery regression. Two new cases execute the real package Vitest segment against isolated repository fixtures. They fail because archived copies are executed alongside the canonical files; two existing isolation tests pass. The exact one-line package proposal preserves every required test argument, but the PDI hook rejected it before application. package.json remains unchanged; no alternative exclusion/config change or executor was used. The new regression source is preserved in `scripts/__tests__/vitest-output-exclusion.test.ts`, with RED, proposal and raw receipts in `output/android-103ms/release-discovery-20260906/`. There is no GREEN or new full-suite result after these two additional tests.

Fresh typecheck, PDI diff and 83 existing AuthGate/Android-evidence tests passed. Scoped lint was rejected, Snyk remains unavailable, and security restrictions were not modified. Source review also found the existing localhost-only empty-state benchmark route: account configuration is not necessarily needed for that diagnostic scenario. Its existing preparation script was rejected before execution, and Chrome DevTools exposes only about:blank rather than the native WebView. No runtime or user data changed. Earlier APK, Git staging, visual, motion and physical-phone blockers remain; T008, T009, T015, T018 and T019 stay open.

## Owner-requested work snapshot — 2026-09-06 UTC

The owner requested APK installation, commit/push and completion of the current plan. The Git snapshot is classified as WIP. T008, T009, T015, T018 and T019 remain open; no release or Android acceptance follows from publishing the branch.

The normal explicit 50-path `git add` was rejected before execution by the unchanged PDI hook: it recognizes protected paths in the command text and refuses shell mutation. The index remains empty and HEAD remains `939daa7e117331ef75201cff3a2c74d314861be2`; no commit or push occurred. No alternate staging, executor or guard modification was attempted. The exact refusal and fresh state receipts are in `output/android-103ms/commit-preflight-20260906/git-stage-block.json`.

The fresh full unit command completed with exit 1: 10,220 passed, 8 failed, 23 skipped and 7 unfinished tests. All eight failed assertions are the preserved Stop mixed-scope regression proposal in `scripts/__tests__/android-visual-runtime-hook.test.ts`; the production Stop classifier remains unchanged. This snapshot retains the RED material without applying the unapproved classifier candidate or suppressing its assertions. The earlier full-suite receipts below keep their original scope and do not represent this tree.

`npm run ci:preflight` also completed with exit 1. Source production-data-integrity passed with zero errors/warnings, and the workspace suite passed 374 tests. The next release-contract invocation discovered archived fixture files under `output/android-103ms/tmp/`: 840 fixture suites failed with `No test suite found`, while 40 real test files and 861 tests passed. Later preflight stages did not run. Archives, test selection, exclusions and enforcement were not changed to obtain a different result. This discovery failure is separate from the eight full-suite RED assertions.

The normal Gradle command remains rejected before execution. The native source backport therefore has no new built or installed APK; retained Android captures concern the earlier unpatched dependency. Technical readiness is FAIL for this complete tree, candidate Visual Runtime and Motion are UNVERIFIED, retained emulator Motion is FAIL, and Artistic/Craft, Model and Plan execution remain UNVERIFIED. Web/Vite, installed PWA, iOS/WKWebView and Desktop/Tauri have no new runtime acceptance from these checks. Local receipts are `output/android-103ms/commit-preflight-20260906/full-tests.json` and `ci-preflight.log` in the same directory.

## SystemBars source backport — 2026-09-06 UTC

The owner explicitly requested installation of the native patches without visual regression. The disabled DOM-ready and inset-listener guards from Capacitor PR 8481 were backported to the existing 8.3.3 boolean implementation. Eight Java lines were added to the lane's actual installed dependency, and the change is preserved in `patches/@capacitor+android+8.3.3.patch`. Versions, package manifests, auth configuration, CSS, canonical assets and enforcement decisions were not changed.

The complete actual SystemBars class was compiled and executed under the existing JBR against isolated Android/Capacitor boundary doubles. Before the edit, four disabled-owner/DOM assertions failed while six enabled-mode controls passed. The final eleven-case suite also replays the actual patch-package artifact from a pristine source independently pinned by SHA-256. Related suites passed 43 tests across seven files. Typecheck, check:all, lane PDI and scoped Oxlint completed; the latter reports 37 warnings outside the new files, while npm audit retains two moderate advisories. These checks do not establish native visual parity or removal of the 189 px displacement.

Source SHA-256 changed from `4a59775c840cc887858f5aa02c17707dee6c0578f036e8d0834cc08c098d0cdb` to `ab01074d737ae8aa58e41bb0a4e39c3feed5abd03f7f5bc70240b157e8790150`; patch SHA-256 is `8dd9661828d21e2157ed5c9940cd4e2cdcc02b4e3e83c61336c61307fb189911`. The initial malformed patch hunk and whitespace findings were corrected and retained in the evidence. See `output/android-103ms/system-bars-patch-20260906/receipt.json`.

The ordinary `android/gradlew -p android :app:assembleBenchmark --console=plain` command was rejected before execution by workspace-guard as unknown shell execution. No new APK was built or installed, no alternative executor was used, and no guard was changed. Fresh installed identity, full current-tree/Snyk checks and candidate visual/motion acceptance remain UNVERIFIED. Existing APK/video/trace evidence is now explicitly baseline evidence for the unpatched native dependency. The next source manifest must include the new patch and patched native source. T008, T009, T015, T018 and T019 remain open.

## Exploratory native entry QA — 2026-09-06 UTC

Typed Android MCP QA continued on emulator-5560 with actual scroll, theme, privacy-link Back, Home/resume and root-Back/launcher actions. System theme and the initial bottom-scroll position were restored. No user record, credential, runtime source, dependency, hook or system setting was changed. The source APK SHA-256 remains `6f7219517276a6b24f1bc38592f19ad76e840a054a6ab7f3176fba1230373b79`.

Five bottom-scroll return sequences expose the same 189 px shift: theme controls move from y=479 to y=668, and legal links move from fully visible bounds to a clipped bottom edge. It occurs after both browser Back and Home/resume. A separate non-recording native counter interval added 32 frames, including 13 janky frames; cumulative percentiles are not per-interaction latency. This is a new observed continuity failure, not a native fix. See `output/android-103ms/entry-ui-qa-20260906-0403/observations.json` and its before/after layout viewer.

The earlier request for the owner simply to sign in is superseded: the visible Arabic warning corresponds to the `!supabase` branch in AuthScreen, which disables provider controls. A working configured QA entry is required before the main drawer routes can be tested. The exact missing configuration value was not read or changed.

Local Capacitor Android/core are 8.3.3. The inspected SystemBars implementation still registers a parent-padding listener despite `insetsHandling: disable`; upstream issue 8466 and the Android changelog identify a correction in 8.4.0. The published reproduction concerns API 29/IME, so its causal connection to this API 36 resume shift remains UNVERIFIED. `UPSTREAM_FINDING.md` records the immutable local sources and next one-change comparison; no upgrade or backport was applied.

Two native-device framebuffer recordings were preserved locally, with exact bytes and hashes. They are not captures of the macOS Emulator window and have no full decode/playback or human acceptance proof. Original PNG archival exceeded the unchanged 1 MiB hook-input limit; a bounded viewer uses fresh native-resolution JPEG-90 MCP stills for geometry only. Recent filtered crash/tile reads returned no matches but do not establish complete package-bound diagnostic windows. Fresh source contracts passed 23 tests; full current-tree, physical-phone and accepted motion evidence remain UNVERIFIED. T008, T009, T015, T018 and T019 stay open.

## Journey collector continuation — 2026-09-06 UTC

The owner requested continuation of the approved plan after the structured-edit repair. The existing locked lane, dirty work and APK were preserved. Current collector changes are limited to `scripts/android-motion/run-real-user-journey.mjs`, its declaration file and `scripts/__tests__/android-journey-runner.test.ts`.

The real runner, executed with isolated external ADB/CDP transport, reproduced three initial failures: open-drawer admission, missing partial JSON and a shared-log clear request. Additional REDs reproduced existing report/screenshot reuse, a falsy rejection becoming success and a missing pending-action record. The collector now rejects the invalid initial state, writes actual partial observations before rethrowing, labels the attempted input UNVERIFIED, reserves output exclusively and preserves both shared-log windows. Snapshot-prefix mismatch or a missing watermark produces null; shared-buffer counts remain distinct from required package diagnostics.

The final collector suites passed 74 tests in two files; neighboring navigation and commit-typecheck suites passed 102 tests in four files. Typecheck, canonical-orb guard and lane PDI passed. Standard lint passed but excludes scripts. The initial missing declaration produced a real typecheck failure and was corrected without suppression. Snyk remains UNVERIFIED because the tool is absent and existing CLI restrictions are unchanged. No new full-tree, native-motion, security or release acceptance is claimed. Raw RED/GREEN outputs, hashes and typed device readings are in `output/android-103ms/journey-repair-20260906/receipt.json`.

Fresh MCP reads still show only emulator-5560, package 2.1.2 / 39 and Arabic sign-in. The owner was asked once to complete sign-in personally while local checks continued. No device input, app record, preference change, restart or additional diagnostic command was performed. The candidate file still hashes to `6f7219517276a6b24f1bc38592f19ad76e840a054a6ab7f3176fba1230373b79`; package metadata is not a fresh installed-byte hash. The runner still uses its existing English day-Orb scenario. Main-flow access, repeated native capture and physical-phone evidence remain prerequisites for further attribution. T008, T009, T015, T018 and T019 remain open, and retained emulator Motion remains FAIL.

## Structured edit repair — 2026-09-06 UTC

The owner's direct request «окей сделай ремонт» authorized the narrow parser correction. This supersedes the structured-code-blocked statement in the earlier CLI evidence section. Normal function-containing patches are now accepted by the actual existing CLI. Actual diagnostic commands remain restricted; Stage B, the separate Stop classifier candidate and hook registration were not changed.

The repair changes only the returned command field in `scripts/codex-governance/tool-targets.cjs` and adds sixteen permanent guard controls. Normalization applies to recognized edit tools with extracted targets and no secondary `cmd`; real shell events and unsupported edit shapes retain their command analysis. Initial RED and additional edge-case RED were followed by 493 passing tests in the workspace, change-governance, Spec Kit and PDI suites. Typecheck and fresh lane PDI passed. Standard lint passed but excludes these scripts. Snyk, the local security suite and the enforcement-health command were denied before execution and remain UNVERIFIED.

Observed receipts, before/after hashes, interim failures, exact scope and the applied diff are in `output/android-103ms/guard-repair-20260906/receipt.json` and `applied.patch`. No application source, APK, canonical visual, user record, video, command allowlist or actor/lane rule changed in this repair. The earlier full-tree and device receipts retain their dates; no new global technical or motion acceptance follows. The separate Stop candidate regressions remain untouched. T008, T009, T015, T018 and T019 remain open.

## Later CLI evidence — 2026-09-06 UTC

This section supersedes older statements below about the connected-device inventory, current UI and available diagnostics. Earlier test, build and capture receipts retain their original dates and scope.

The existing CLI session reread the requested 17:04 handoff and current plan/tasks/convergence, then refreshed output/android-103ms/CLI_RESUME_ACK_2026-09-05_1704.json while preserving its earlier acknowledgement. A fresh workspace doctor confirms the same locked lane and source HEAD. No new task, branch, worktree, agent or publication was created.

Fresh typed MCP reads show one connected target, emulator-5560, with the Arabic sign-in screen focused. The 17:04 Mood-select state is historical. The later native QA had exercised empty-note IME, Back, reduced-motion routes, Huge text and Arabic selection; its receipt is output/android-103ms/native-mcp-ui-qa-20260905.json. The subsequent entry-theme check restored System mode. Source and test review show that Android's fixed light system-bar contrast is deliberate current behavior, so the static night-area observation was not promoted to a visual regression or changed.

The retained evidence index is output/android-103ms/evidence-packet-20260905/android-visual-runtime-evidence.json. Its assembly verified 123 referenced hashes, including two window videos, three Perfetto captures and ten independent installed-APK observations. That integrity result does not refresh capture dates or establish motion acceptance. Full video review, physical-device evidence, a new full source manifest and signer verification remain unavailable.

The full-route video log also contains a 1683 ms HWUI frame and GL_INVALID_ENUM. The 1670.421834 ms interval from IssueDrawCommandsStart to SwapBuffers is wall time inside the graphics portion, not measured GPU execution or input-to-display latency. See output/android-103ms/full-route-render-stall-analysis-20260905.json. Its temporal association with the Habits transition is not a proven renderer-disposal cause.

Current runner inspection adds three evidence-quality findings within the existing T018/T019 work: initial admission checks positive selection texts but does not reject an open drawer; a thrown journey failure prevents the final journey JSON from being written; screenshot mode clears shared logcat twice. The recording wrapper uses video-only mode and avoids those log-clearing branches. No log buffer was cleared in this continuation. The source hashes, retained failure and proposed regression cases are in output/android-103ms/journey-harness-characterization-20260906.json. No new automated RED/GREEN or code change was performed for these findings.

The earlier structured-code and diagnostic-command refusals remain unresolved and were not retried or bypassed in this continuation. The source hashes of the workspace restrictions remain unchanged. Available read-only attribution and state reconciliation have continued; a new main-flow capture still requires approved QA access and permitted diagnostic execution.

Current quality: Technical UNVERIFIED for the complete current tree; Visual Runtime UNVERIFIED; Motion FAIL from retained emulator measurements; Artistic/Craft, Model, Plan execution and Release UNVERIFIED. Web/Vite, installed PWA, Android, iOS/WKWebView and Desktop/Tauri runtime are unchanged by this evidence-only continuation. T008, T009, T015, T018 and T019 remain open.

## Earlier implementation and receipts

As of 2026-09-05, the scoped code and tooling changes are implemented. The candidate is installed on an isolated API 36 emulator. Repeated emulator motion measurements **FAIL**; overall phone acceptance remains **UNVERIFIED**. The 103 ms goal has not been achieved or demonstrated on a phone. No commit, publication or release was performed.

Editing lane: `codex/android-103ms-20260904`, HEAD `939daa7e117331ef75201cff3a2c74d314861be2`. The original checkout was rechecked clean on `main` at `891804719ee03fcdcf4ddedc4e71b041748cb9f0`.

## Implemented behavior

- `DrawerV2.tsx` releases a reduced-motion close immediately, including preference changes during closing. Normal transform completion retains ownership; reopening invalidates stale callbacks. The existing 1000 ms exceptional watchdog remains.
- `useNavigationV2.ts` begins Android drawer closing without the earlier pre-close double-rAF delay. It retains the post-unmount compositor boundary and request identity before mounting a destination.
- `src/index.css` scopes the Android panel/backdrop and entrance to 48 ms, control effects to 80 ms and entrance delay to zero. Colors, layout, blur, canonical orb code and production data remain unchanged.
- `.husky/pre-commit` calls the actual application and tooling typecheck. Its isolated real-TypeScript negative and positive controls pass.
- Android capture requires independent installation observations, binds environment and installation to one device, hashes the environment artifact and preserves the device recording on failed/partial transfers. Two review findings were reproduced, corrected and verified with 53 focused tests.
- Perfetto analysis schema 2 separates app work from displayed updates. Display cadence uses distinct app-linked SurfaceFlinger completion timestamps, includes all lifetimes of the target process, excludes dropped/incomplete display slices, and returns null FPS for missing links or insufficient samples. Six SQL execution regressions extend the evidence suite to 59 tests. Schema 1 `frameTimeline.presentedFps` and its app-start gap field must not be interpreted as presentation evidence.
- Existing instructions, context/completion checks and RAG routing reference `ANDROID_INTERACTION_BUDGET.md`. The deploy workflow adds the five interaction/evidence/typecheck-control suites before build; the exact command passed 155 tests locally. Hosted CI has not run for this patch.

## Evidence and limitations

Receipts and raw artifacts are local under `output/android-103ms/`; they are ignored evidence, not shipped application data. Each command receipt contains its real exit code and log path.

| Claim | Fresh evidence | Result and boundary |
|---|---|---|
| Reduced-motion regression | `drawer-reduced-valid-red.json`, `drawer-reduced-green.json` | Expected RED; corrected component/hook integration GREEN |
| Android scheduling and re-entry | `android-close-scheduling-red.json`, `drawer-reentry-red.json`, `navigation-reentry-green.json` | 99 tests / 3 files GREEN |
| Real hook type errors | `precommit-typecheck-red.json`, `precommit-typecheck-green.json` | 3 tests GREEN, including both referenced TS projects |
| Independent evidence and preserved transfer | `review-evidence-clean-red.json`, `review-evidence-green.json` | 8 expected failures before; 53 tests GREEN after |
| Full unit suite | `full-unit-final-clean.json` | Exit 0; 860 passed files, 1 skipped; 10,153 passed tests, 23 skipped and 7 pre-existing unfinished tests |
| Existing CI command plus added suites | `ci-interaction-step.json` | 155 tests / 5 files GREEN locally; hosted CI UNVERIFIED |
| Types, lint, translations, visual guards | `final-check-all.json` | Exit 0; 8 locales / 3644 keys; existing color scanner reports 10 unchanged findings under its existing threshold |
| Source and bundle integrity | `data-integrity-source.json`, `bundle-data-integrity.json` | Exit 0, zero errors/warnings; 769 reachable source files |
| Canonical visual source | `canonical-orbs.json`, `final-check-all.json` | Canonical orb and logo guards pass; source/asset models were not replaced |
| Browser journeys | `browser-phase2-green.json`, `browser-rtl-large-text.json` | 3 normal/reduced/configuration tests and 2 ar/he large-text tests passed |
| Broader browser behavior | `chrome-performance-base-corrected.json` and its report | 14 ready routes across phone-sized/desktop viewports, zero console/request/response errors; existing smoke budget is not 103 ms acceptance |
| PWA / WebKit regression | `pwa-offline-cross-engine.json` and its report | 2 passed, 2 intentional engine-specific skips; Chromium service-worker offline reload/write persistence and WebKit diary controls verified in isolated local profiles |
| Snyk | `security-final-scoped-ai.json`, `snyk-final-source-manifest.json` | Scoped 11-file scan, SARIF 1 run / 0 findings; no whole-system security claim |
| Build identity retained | `apk-build-input-recheck.json` | All 14 source/build input hashes still match the Android build manifest after tooling/docs review |
| Native candidate | `android-assemble-benchmark.json`, `isolated-emulator/install.json`, per-run independent installation observations | Exact candidate installed on emulator-5560; before/after installed SHA-256 agrees across three traces and the video run |
| Display-frame analysis | `display-frame-sql-red.json`, `display-frame-sql-green.json`, `display-frame-real-trace-01.json` through `03.json` | 7 expected RED failures, then 59 tests GREEN; corrected analyzer replayed all three real traces with matching counts, cadence and gaps |
| SQL negative controls | `display-frame-mutation-controls.json` | Four deliberate counting mistakes detected: start timestamps, frame/interval off-by-one, duplicate timestamps and unrelated app frames |
| Current CI regression command | `display-frame-ci-regressions.json` | 161 tests / 5 files GREEN after the analyzer correction; hosted CI remains UNVERIFIED |
| Tooling type/lint follow-up | `display-frame-typecheck.json`, `display-frame-lint-scoped.json`, `display-frame-lint-classification.json` | Application/tooling typecheck passes. Standard ESLint excludes these scripts; an additional expanded profile fails on two lines already identical in HEAD: intentional rest-destructuring omission and an unnecessary test-string escape. No new finding in the display-query patch; expanded-profile lint is not claimed clean |
| Tooling security follow-up | `display-frame-security-ai.json`, `snyk-display-source-manifest.json` | Scoped four-file Snyk scan: SARIF 1 run, 0 findings; earlier 11-file scan does not by itself cover these later edits |

The first full suite found only a stale generated motion inventory: 10,144 tests passed, 1 failed. The generator was refreshed, preserving all 1,145 existing owners and adding 3 Android CSS owners. `cap copy ios` regenerated the previously absent ignored native config so iOS inventory coverage was retained; no tracked native file changed and no iOS runtime claim follows. The focused inventory suite then passed. The next full run (`final-full-unit.json`) failed during discovery because task scratch fixtures under `tmp/103` were picked up as tests: 526 fixture suites failed while 10,161 discovered tests passed. That run is not PASS. The complete scratch directory was atomically moved, preserving all files, into the existing `output/` tooling boundary; `temporary-directory-relocation.json` records this correction. No Vitest exclusion was changed. The next full run (`full-unit-corrected-scratch.json`) exposed the fixture worktree guard: 23 tests correctly rejected creation beneath `output/`; 10,130 passed. This was also an invalid scratch location, not a GREEN run. The runner now uses a fresh empty per-command directory under `tmp/`, with the existing Git discovery ceiling, and archives it under `output/` only after the command finishes. This preserves all fixtures without changing production guards or test discovery. `workspace-tests-clean-scratch.json` verifies the affected suite with 51 tests GREEN before the final full rerun (`full-unit-final-clean.json`), which completed with exit 0: 860 passed files, 10,153 passed tests, 23 skipped and 7 pre-existing unfinished tests. The corrected runner archived its scratch files after completion.

Earlier setup trials are retained with their actual failures. The first performance smoke used the wrong local base URL and was stopped with exit 130; the corrected `/people-first-app/` run completed successfully. The first direct Vitest review probe also discovered a scoped scanner copy under `output/`; the repository test command, which already excludes `output/**`, produced the clean RED/GREEN controls. No test or scanner exclusion was added to suppress a failure.

## Measured browser diagnostics

The same Chromium CSS branch was exercised at 390 × 844. It uses the web scheduling branch, not the native bridge/compositor. Ten samples per motion mode cover first and warm visits to all five destinations. These observations are diagnostic; neither their DOM nor rAF timestamps prove display presentation. The pre-timing baseline already includes the reduced-motion correctness fix.

| Metric, normal motion | Before p50 / p95 / p99 / max | Candidate p50 / p95 / p99 / max |
|---|---|---|
| Click → drawer absent | 369.8 / 1195.8 / 1195.8 / 1195.8 ms | 137.1 / 394.9 / 394.9 / 394.9 ms |
| Click → destination DOM ready | 460.6 / 3096.6 / 3096.6 / 3096.6 ms | 239.3 / 1496.7 / 1496.7 / 1496.7 ms |

Candidate reduced-motion drawer removal: p50 36.9 ms, p95/p99/max 90.3 ms. Candidate reduced-motion DOM readiness still reaches 1115.7 ms. Counts are 10/10 per mode with no missing observations; first visible response and actual final presentation were not measured. See `browser-diagnostic-summary.json` for unrounded values and individual observations. **These results do not satisfy an overall 103 ms claim.** A loaded route module is not a prepared GPU-rendered screen.

## Installed Android frame and video diagnostics

The earlier storage failure is resolved without deleting recordings or clearing app data. The existing emulator-5554 and its old APK remain intact. A separate AVD was created inside ignored task output, using the already installed API 36 arm64 image. Its configuration hash is `312a090de5678ce3b91fe8510cfb6422c60d44c37d031d1f1bfe5981683aba6a`. This is emulator regression evidence, not physical Pixel/OEM proof.

Target: emulator-5560, 1080 × 2400, 420 dpi, 60 Hz, WebView 133.0.6943.137. All three Android animation scales are 1. The installed benchmark APK SHA-256 is `6f7219517276a6b24f1bc38592f19ad76e840a054a6ab7f3176fba1230373b79`. Source commit and all 14 build input hashes were rechecked in `isolated-emulator/source-current.json` and `build-input-recheck.json`. The benchmark is release-derived and profileable; its inspection support is not a claim about the final store artifact.

The scenario uses the existing localhost benchmark route with no authenticated account or fabricated session. Each pass performs eight UI-tree-derived actions around the drawer, Mood and light/dark theme. The three 90-second Perfetto captures are separate from screen recording and task CDP inspection. They include steady scene time around the interactions; these averages are not input-to-final-presentation timings.

| Pass | Distinct displayed updates | Updates/second | App deadline misses | Longest display interval | Display intervals >103 ms |
|---|---|---|---|---|---|
| 1 | 3457 | 38.443546 | 47.41105% | 282.071501 ms | 5 |
| 2 | 3350 | 37.255595 | 52.656716% | 309.519417 ms | 7 |
| 3 | 3375 | 37.529181 | 52% | 233.918250 ms | 5 |

Every app display token in these traces has a matching completed SurfaceFlinger frame. Positive trace error/data-loss counters and incomplete app slices are absent. The counts come from `isolated-emulator/perf-drawer-0{1,2,3}/analyzer-v2.json`; raw traces and independent SQL remain preserved alongside them. Display timestamps use SurfaceFlinger `ts + dur`, with duplicate timestamps removed and `(updates - 1) / span` cadence. Display-interval percentiles use nearest rank; app-work percentiles retain Perfetto's percentile function. See the [FrameTimeline documentation](https://perfetto.dev/docs/data-sources/frametimeline) for the app/display distinction. An emulator's virtual display is not the host recording frame rate or a physical phone display.

Android MCP `doctor` and `get_gfx_info` returned fresh typed results, saved in `isolated-emulator/android-mcp-read-proof.json`. The doctor warns that arbitrary shell execution is disabled; that permission was not changed. Advanced tracing uses the existing local ADB CLI. `gfxinfo` agrees that frame deadlines are missed, but its counter window differs from the exact Perfetto window and is not mixed with those counts.

The separate 120-second continuous recording is `isolated-emulator/visual-drawer-01/window.mov`, SHA-256 `188a0e635f48580909fd92506362e09cb12839b0e6f8f5397d8f49bd6d4a9346`. Its eight actions and thirteen checkpoints completed. Phase stills locate moments in the recording; they do not prove full motion quality. PID-scoped logs have no observed tile-memory, context-loss, crash or ANR messages during the recorded passes. This does not cancel the measured frame failures.

A later full-route run, `isolated-emulator/visual-full-route-03/`, completes 24 semantic actions and 36 checkpoints across Mood select/refine, the mood slider, Android Back, Habits, Diary, Planning, Settings and theme changes. Its continuous 210.02-second video contains 10,444 fully decoded samples, SHA-256 `61471b24b9b6aa2b9fb1b6900cb9920d0437a9d51ebff324ab3fe064141c8cff`; independent APK hashes agree and the app PID is stable. The earlier drawer video also fully decodes: 5,839 samples / 120.101667 seconds. These are recording sample counts, not application FPS. See `full-route-video-decode.json` and `drawer-video-decode.json`.

Both failed full-route setup attempts are preserved. Attempt 01 stopped at device timestamp collection before recording or interaction. Attempt 02 began with the drawer already open; Android's accessibility tree exposed underlying controls, so the script's initial text checks were insufficient and it later failed at refine readiness. Its video confirms the invalid starting state. Attempt 03 first used Android MCP to select Mood and verify that the select controls were present and Close menu absent, then ran the unchanged journey. This proves the completed semantic scenario, not continuous visual or timing acceptance. A brief loading surface is visible around Android Back; full motion quality remains unresolved. Native normal/reduced-motion repetition, RTL, large text, IME and lifecycle matrices are still incomplete.

Run 1 phase attribution points to heavier rendering in the light theme and open drawer: approximately 41 updates/second on the light Orb, 29 with its drawer open, 57 on the dark Orb and 37 with its drawer open. This is a diagnostic lead, not an established cause. CDP LayerTree inspection confirms the existing backdrop partition already clips the exposed strip; a supposed full-screen backdrop allocation is not supported. Canonical materials and effects have not been reduced. Host GPU contention and emulator behavior remain limits on absolute phone predictions.

## Visual Integrity Critic: STOP

Technical: PASS — 10,153 unit tests, focused regression, type/lint/build, source/bundle integrity and structural visual evidence above.

The later analyzer correction has its own 59-test GREEN, typecheck, three real-trace replays, four negative controls and scoped Snyk evidence. The earlier full-suite receipt is retained with its original scope and count; it is not relabelled as a new full run after the tooling edit. The extra lint profile's two pre-existing findings remain explicit above.

Visual Runtime: UNVERIFIED — Chromium evidence and exact installed Android drawer/theme and full-route semantic journeys exist; native safe-area, RTL, IME, lifecycle repetition and full continuous-motion acceptance remain incomplete.

Artistic/Craft: UNVERIFIED — English before/after menu screenshots share SHA-256 `69a493054a25e3d392a3cd9207829c5160520b84580367ca5f08ac92717ee0d8`; direct inspection found matching layout/materials. Arabic/Hebrew large-text screenshots retain readable, correctly mirrored controls. Human review of changed motion is absent.

Motion: FAIL on the API 36 emulator — three independent traces reproduce missed deadlines and display gaps over 103 ms. Physical-phone interaction timing and final presentation remain UNVERIFIED.

Model: UNVERIFIED — no model or generated media asset changed. Existing model approval is not transferred to this navigation timing candidate.

Plan: PASS — owner-approved scope, exact baseline, failure-first tests, rollback and missing-evidence gates are explicit. This is plan quality, not plan completion.

What is wrong:
- The evidence cannot establish complete phone interaction within 103 ms or absence of native flicker/jank.
- The installed candidate reproduces poor frame pacing; the exact rendering cause and complete input-to-final-presentation latency remain unresolved.

Required fixes before final:
- Complete T018/T019 against independently verified installed artifacts and actual device presentation.

Do not claim complete because:
- A still screenshot and a successful structural check cannot prove motion quality or physical-phone behavior.

Next best action:
- Attribute the repeated light-theme/drawer rendering cost, complete the native route/accessibility/lifecycle matrix, and rerun any scoped fix against the same traces and continuous visual checks. Physical-phone acceptance additionally requires a connected phone.

## Platform and domain matrix

| Target/domain | Impact | Current evidence status |
|---|---|---|
| Web/Vite | Shared reduced-motion and re-entry correctness | Chromium journeys/layout and 14-route smoke verified; full cross-browser runtime UNVERIFIED |
| Installed PWA | Shared navigation ownership | Chromium service-worker offline reload/write persistence verified; standalone flag is emulated, actual installation/lifecycle runtime UNVERIFIED |
| Android/Capacitor | Primary timing and ownership change | Exact candidate installed; drawer/theme and 24-action full-route journeys verified, repeated emulator motion FAIL; full matrix and phone acceptance UNVERIFIED |
| iOS/WKWebView | Shared reduced-motion/re-entry; Android CSS remains scoped | WebKit diary readiness/controls and generated config verified; actual WKWebView/native safe areas UNVERIFIED |
| Desktop/Tauri | Shared hook behavior | Desktop Chromium smoke verified; packaged Tauri runtime UNVERIFIED |
| Store/Release | No publication | UNVERIFIED; no release requested or performed |
| Accessibility | Reduced motion, focus, 44 px targets, RTL, text scaling | Unit/Chromium subset verified; native Back/IME/resume/notches UNVERIFIED |
| Performance | 103 ms finite-interaction target; frame pacing separate | Corrected frame analyzer verified; repeated emulator frame pacing FAIL; overall phone target UNVERIFIED |
| Security And Privacy | Local tooling integrity; no business-data changes | Source/bundle scans and scoped Snyk verified; no private account/device-runtime security claim |
| Testing | Regression and CI wiring | Focused suites, full unit run and check:all verified; skipped cases remain explicit |
| Operations | Single locked lane, preserved original checkout and installed data | No reset, stash, force push, deployment or prior-video deletion |

## Preserved state and remaining device evidence

Candidate: `output/android-103ms/android-candidate/app-benchmark.apk`, SHA-256 `6f7219517276a6b24f1bc38592f19ad76e840a054a6ab7f3176fba1230373b79`, package `com.zenflow.app`, version 2.1.2 / 39, installed on emulator-5560. The previous APK on emulator-5554 has backup SHA-256 `c06f6b2341abd04c7e51d4b0ad8e500fccfa9a458d52160babcdb60f9e022f14`; it must not be labelled as the candidate.

The previous deletion question is superseded: installation and profiling now work on the isolated emulator, so deletion is unnecessary. The five old recordings, totalling 308,984,498 bytes, remain intact. Their backup/hash manifest is `output/android-103ms/emulator-video-backup/manifest.json`. No elapsed time or absence of an answer was treated as deletion approval.

Convergence reviewed 9 FRs, 5 success criteria, 9 acceptance scenarios and 5 plan decisions. Two HIGH partial findings were appended as T018/T019 without rewriting earlier tasks. Constitution status is PROPOSED / not ratified / nonblocking; it produced no additional remediation.

The PWA follow-up uses the unchanged production web build in `output/android-103ms/phase2-web-dist`, localhost HTTPS and isolated test profiles with tracking disabled. Its standalone flag is explicitly emulated. No user account or external data write is used; this is not installed-phone evidence. The latest ADB inventory contains emulator-5554 and emulator-5560, with no physical phone. No additional deletion approval is needed to continue the available emulator diagnostics.
