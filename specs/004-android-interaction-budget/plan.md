# Implementation Plan: Android interaction budget

## Stable-root native characterization — 2026-09-09

T029 completed the matched real-app comparison: baseline-1 has22 strip frames in4 episodes; stable-root-1 and stable-root-2 have0 among2288 and2346 frames. All use app3190bcf3/testbb0c4eb9, four real hide/refocus cycles each, independent installed hashes and empty-field checks. Full boards and baseline strip frames were visually inspected. Manual native scroll with IME visible retained Back, Save, Tools and the body. This is a successful boundary characterization, not production/motion/presentation acceptance.

T030 uses an opt-in lease in the existing SafeArea owner, acquired only by a mounted non-desktop Android JournalEntryEditor. Native-generated owner IDs prevent stale asynchronous cleanup from releasing a replacement. Preserve the original soft-input state bits and restore the exact mode on matching release, page navigation or destruction; reject queued calls after teardown. While active, reserve bars/cutout natively, forward zero handled IME/system dimensions and send only the measured numeric overlap divided by the actual devicePixelRatio to CSS. A native-owned root marker scopes full-height/padded layout to the dialog; desktop and other forms retain the legacy path. No per-frame layout loop, new plugin/dependency, style replacement or data mutation. Native API/geometry and hook race regressions precede production edits; new owner and installed patch must replay byte-exactly. Subresource network errors cannot release an editor lease: Capacitor exposes those through the same error callback as main-document failures, so page-start/destruction are the reliable reset boundaries.

Proceed under the owner's new native-boundary authorization in FR-019/FR-020, SOLO in the existing lane. The controlled fixture keeps the root background and WebView at full height and reserves real IME overlap inside the editor. Two recordings contain zero detected strips across 4709 decoded frames; its same-APK legacy control contains one. The pure adjustNothing control still contains a detected strip, so changing that flag alone is not the proposed repair. These are fixture observations, not ZenFlow acceptance.

First extend only `android/app/src/androidTest/java/com/zenflow/app/ImeAnimationDiagnosticTest.java` with an explicitly selected stable-root mode. Replace the test Activity's inset listener, use system-bar/cutout native padding, forward zero handled IME dimensions, set adjustNothing and inject test-scoped full-height editor/paper geometry using the actual numeric overlap. No animation-frame CSS updates, native recoloring, ancestor unclipping, background suppression or user-record access. Keep baseline mode unchanged. Finish the test Activity, remove temporary CSS/callbacks and restore its soft-input mode in cleanup; retain independently identified original app/test APKs for data-preserving restoration.

The comparison must exercise the same built application bytes, first baseline then candidate, with four real blank-editor hide/refocus cycles per capture, independent package hashes and native geometry. If the candidate still drops content, reject it before production editing. If it succeeds, first add native/layout regression RED for the new owner and apply the smallest production correction in the existing SafeArea patch and Android-scoped editor layout only; retain all common floating/docked/hidden/version/backdrop/Back assertions. Re-run exact visual comparison, three warmed runs/ten cycles, RTL/Huge/motion/theme/resume coverage and separate presentation evidence before broad CI and integration.

Platform/domain matrix: Android native/CSS geometry is the intended eventual impact; current writes are tests/docs only. Web/Vite, installed PWA, iOS/WKWebView and Desktop/Tauri runtime are unchanged by this diagnostic and whole-goal proof remains UNVERIFIED. Accessibility preserves 44 px controls, focus, Back, RTL, scaling and safe areas. Performance keeps full-quality effects and separates diagnostic observation from 103 ms final-presentation proof. Security/privacy keeps numeric insets and empty-field evidence only, with source-scoped scanning; no network, storage or permission changes. Operations preserve installed data, keyboard settings and all inherited lane edits. Store/release/main integration remains pending. Roll back only this bounded addition, never the prior floating-IME or Back fixes.

The freshly read constitution is PROPOSED/unratified/nonblocking; extensions and automatic hooks remain disabled. Existing feature artifacts are extended without a new feature, branch or worktree. Full details and rejected alternatives: [native ownership investigation](../../docs/superpowers/plans/2026-09-09-native-ime-inset-ownership.md).

## Approved Diary Orb Background — 2026-09-08, 15:09 UTC

The owner authorized the V2 Diary static-background replacement, with all existing Orb day/night nuances. Follow [the bounded implementation plan](../../docs/superpowers/plans/2026-09-08-diary-orb-background.md) and FR-010–FR-014 in spec.md. This extension uses a presenter-supplied page background in JournalModule, preserving the feature-to-page dependency direction, all journal data/security logic, existing V1 defaults and the canonical Orb source. No global renderer relocation or second hidden page is included.

The fresh baseline passed DiaryPage.audio and JournalModule.handoffBehavior: 2 files, 69 tests. Source identity and all 72 inherited dirty paths are recorded in `output/android-103ms/diary-orb-background-20260908.fmRc1u/source-before.json`. New regression RED and exact-build verification remain to be executed; background reuse alone makes no responsiveness or artistic claim.

Only the emulator is available, as explicitly clarified by the owner. Continue available emulator validation; do not use missing physical hardware as an implementation blocker. Physical-phone performance and untested PWA/iOS/Tauri/release rows remain UNVERIFIED. Earlier dated packets below retain their original scope; open T009/T015/T018/T019 are not silently closed by this extension.

## Offline recovery after XProtect stop — 2026-09-07 UTC, 23:15

Session `01a0760c-2c7c-73f2-b799-d5ecfb22ac01` resumed SOLO in the same locked lane after the owner reported XProtect's 21:54:28 UTC rules-engine denial and emulator termination. The emulator and interrupted CDP chain were not restarted. The exact denied script/URL and causal relationship remain UNVERIFIED; reviewed local diagnostic source is not proof that replay is safe.

All 60 saved dirty file hashes matched before this documentation update. The three navigation suites plus Android evidence suite freshly pass 164 tests. Offline analysis of the retained Settings trace identifies a 46.288 ms RenderThread sync-token wait (69.791–116.079 ms after pointerup), overlapping 40 Chromium GPU-thread tasks and 32 raster-end operations. These are trace wall-time intervals, not hardware GPU time, final presentation, or a proven fix. Negative controls reject missing input, data-loss metadata and missing thread identity.

No production behavior or accepted visuals changed. Runtime recovery is incomplete: reduced motion was last changed from true to false for the interrupted diagnostic; restored preferences, final installed hash and owned-forward cleanup after the incident are UNVERIFIED. The earlier restoration paragraphs retain their dated scope. T009/T015/T018/T019 remain open. Current safe-continuation evidence: `output/android-103ms/offline-recovery-20260907/REVIEW.md` and `trace-attribution.json`. A temporary outside-repository scan-directory creation and the security-suite invocation were denied by guards; neither was retried through another executor.

## Navigation feedback handoff — 2026-09-07 UTC, 16:24

The requested continuation now includes a concrete Android navigation fix: `useNavigationV2.ts` clears outgoing feedback in the destination commit, preserving destination Suspense loading, retained drawer ordering/cancellation and non-Android behavior. The old 320 ms cosmetic hold no longer keeps the Android indicator and its blur surface after handoff. Six new hook cases and stronger destination-loading assertions pass in the actual three source suites: 105 tests. Strict lint, benchmark sync/Gradle and scoped source security checks pass; Snyk reports one run and no findings.

The final built and independently observed installed APK is `50c3f6bfb2a66430aab4b8268d0db849060692f467cbbdf8d5a98e08e30f0aed`, package 2.1.2 / 39. All 21 selected inputs match across packaging. On Arabic/RTL Huge text with reduced motion, 15 native route selections across three passes reach a settled route/pending/fallback DOM state in 38.3–95.8 ms. These observations do not include Orb visual readiness or final presentation. With normal motion, five native selections still take 168.6–211.2 ms; drawer removal alone takes 111.4–139.4 ms. The 103 ms normal-motion gate therefore remains FAIL.

Ordinary Gboard still clips Back/save controls on the final APK. Missing Orb/foreground after repeated route changes also reproduces on the independently reinstalled old `9ac94b0e…` APK, despite ready DOM/canvas state. This rules out the navigation patch as a necessary condition for that defect; it does not establish full visual parity or its cause. The temporary scroll-layer and Vulkan-backend diagnostics were rejected, with original settings restored. No renderer quality reduction or native workaround was adopted.

All seven compared public preferences match their starting values. The app ends on Orb selection; original keyboard settings and Skia OpenGL are restored, and the owned CDP forward/temporary observer are removed. Unrelated initial dirty work is preserved. Current evidence and the final CI/browser results are linked from `output/android-103ms/software-convergence-20260907/REVIEW.md`. T009/T015/T018/T019 remain open for the actual visual/frame, normal-motion/presentation and physical-phone requirements; a new report does not close them. All older packets below retain their dated scope.

## Daylight draw-dispatch applied — 2026-09-07 UTC

The owner explicitly approved both proposed paths. The reviewed `useAndroidDayLargeEffects.ts` patch is applied; its component test also includes a typed-lint correction to the resize callback. The generated motion inventory was refreshed after Android synchronization with unchanged discovery counts and 100% coverage.

VERIFIED: the original duplicate-startup regression changed from RED to GREEN; 111 focused checks, 10,240 full-suite tests and seven browser navigation cases passed. The full suite retains 23 skipped and seven pre-existing todo cases. Chrome performance smoke completed 14 route/profile checks with no console or network errors. The exact built and independently observed installed APK is `9ac94b0edec4a221b9ee916792694f0615f9442e7130061af64bec0868f7c015`. Production code and its test passed scoped Snyk with no findings.

Two native Light returns now submit one full draw set per browser timeline tick, compared with three on the prior `4e496f6e…` APK. Four fixed daylight phases (0, 1,000, 5,000 and 12,500 ms) have identical PNG hashes and zero different RGBA pixels at 1082 × 2202. This proves scoped buffer parity and dispatch coalescing, not complete transition or presentation acceptance.

Three separate 90-second frame runs retain 10/2/2 display gaps above 103 ms, with maxima 480.116125/164.366167/133.031001 ms. Host CI ran concurrently; direct baseline performance attribution is UNVERIFIED. The independent native video completed eight actions and 13 checkpoints. Its first day-menu checkpoint lacks the miniature orb; later night/restored-day frames and a fresh warmed opening show it. Causality and reproducibility remain UNVERIFIED, and broad visual parity is not accepted. The earlier IME failure remains unresolved.

Observed public preferences were restored exactly. No physical phone is connected. T009/T015/T018/T019 remain open: 15/19 main IDs checked. Evidence is in `output/android-103ms/day-render-applied-20260907/`, especially `dispatch-comparison.json`, `pixel-comparison.json`, `frame-summary.json`, `visual-review.json` and `ci-preflight-after-inventory.log`. Earlier dated packets below remain historical evidence; the proposal is no longer awaiting permission.

## Frame attribution and IME synchronization — 2026-09-07 UTC

Three saved current-APK traces associate one long display gap per run with Light theme activation. During those draw calls, RenderThread sleeps for 82.38–96.91 ms; Chrome_InProcGp wakes its longer sleeps and is CPU-running for 86.64–115.69 ms. This identifies a rendering dependency, not the underlying Chromium operation or hardware GPU duration. Other gaps occur outside immediate input windows. Clock alignment remains approximate.

A temporary native postVisualStateCallback viewport experiment on the unchanged `4e496f6e…` APK still leaves the Back control blank after ordinary IME resize. It is rejected. Two initial captures are excluded because the handwriting tutorial intercepted the first tap. The three subsequent native captures were inspected; the aligned candidate and restored CSS have identical geometry. All 57 pre-existing dirty files remained unchanged during diagnosis. Public preferences and keyboard settings were restored, and the test package plus ten hash-verified device captures were removed.

No production fix or acceptance checkmark follows. T009/T015/T018/T019 remain open: 15/19 main IDs checked. Physical-phone and full visual/motion acceptance remain UNVERIFIED. Evidence: `output/android-103ms/frame-attribution-20260907/analysis.json` and `output/android-103ms/ime-frame-sync-20260907/receipt.json`.

## Current APK frame and video verification — 2026-09-07 UTC, 03:19

The same installed `4e496f6e1fb9c9ed748e230010e8008a0a6867c39f1aa2377c07301fe9027b2d` APK now has three separate warmed 90-second emulator traces in English/LTR, Huge text (1.5), normal motion and 60 Hz. They link 5,309 / 5,346 / 5,309 frames with no missing display tokens or reported trace errors/data loss. All three exceed the display-gap budget: 1 / 2 / 3 gaps above 103 ms, maxima 116.865834 / 109.980583 / 159.195583 ms. Each native scenario completes eight actions; this does not establish interaction latency. A separate 54.496867-second native video fully decodes to 2,268 frames; six inspected settled checkpoints show Orb/menu controls in both themes. Full transition parity and phone acceptance remain UNVERIFIED. Four independent installation pairs validate; all 18 build inputs match. Arabic/RTL, Huge, reduced motion and auto theme were restored; owned trace copies/forwards were removed after verification. No production code changed. T009/T015/T018/T019 remain open, 15/19 checked. Evidence: `output/android-103ms/current-apk-frames-20260907/receipt.json` and `summary.json`.

## Theme phase diagnosis — 2026-09-07 UTC, 02:55

The unchanged installed APK `4e496f6e1fb9c9ed748e230010e8008a0a6867c39f1aa2377c07301fe9027b2d` was exercised with 24 native theme activations under Arabic/RTL Huge text. Fourteen minimally observed baseline activations take 118.6–297.0 ms from DOM activation to veil cleanup; all exceed 103 ms. These are event/phase measurements, not input-to-photon evidence. Separate trace and animation-state reads identify pending release startup and repeated native Back visibility/layout work, without proving the whole cause. Explicit release-clock startup still exceeds 103 ms in four of six attempts and was rejected. No production change follows. Public preferences were restored exactly, temporary observers and the owned forward removed, and all 18 build-input hashes verified unchanged. T009/T015/T018/T019 remain open; 15/19 main IDs are checked. IME visual failure and physical-phone/presentation acceptance remain unresolved. Evidence: `output/android-103ms/theme-phase-20260907/receipt.json` and `analysis.json` in that directory.

**Branch**: codex/android-103ms-20260904 | **Date**: 2026-09-05 | **Spec**: [spec.md](spec.md)
## Summary
Fix the reduced-motion drawer delay, measure and remove unnecessary Android navigation waits while retaining compositor ownership, repair the actual commit typecheck and extend existing Android evidence contracts.
## Latest execution status — 2026-09-07 UTC

The plan remains 15 of 19 main tasks complete within their written scope. T009/T015/T018/T019 are open. The technical CI blocker is resolved through a behavior-preserving extraction of primary drawer rendering into DrawerV2Destinations, preserving parent ownership and adding no DOM wrapper. The same 99 characterization tests and seven production-browser cases pass. Full uninterrupted CI exits 0, including the unchanged ratchet at three oversized components and score 9.2. The generated inventory and measured architecture count are current; scanner and build evidence is retained.

New APK `4e496f6e1fb9c9ed748e230010e8008a0a6867c39f1aa2377c07301fe9027b2d` was built from an unchanged before/after source snapshot and independently identified after installation and diagnostics. Arabic Huge/reduced drawer bounds pass their scoped checks. Five native destination taps reach their paths and dismiss the drawer; immediate pending/loading states are retained rather than counted ready. Ordinary IME clipping still reproduces. Full-height and CSS invalidation experiments temporarily restore foreground, but an automatic viewport-height prototype fails, so no IME production change follows. Diagnostic cleanup and original keyboard settings are verified. New-candidate repeated motion, 103 ms, physical-phone and human acceptance remain open. Evidence: `output/android-103ms/drawer-structure-20260907/receipt.json` and `output/android-103ms/ime-surface-20260907/receipt.json`.

## Historical execution status — 2026-09-06
The plan is **Android interaction budget**. The [task ledger](tasks.md) records 15 of 19 main tasks completed within their dated scope: T001–T008, T010–T014, T016 and T017. T009, T015, T018 and T019 remain open. This count is not an Android readiness percentage.
Completed substeps are checked in the [detailed plan](../../docs/superpowers/plans/2026-09-04-android-interaction-budget.md). The exact release-command proposal is now applied and its four isolation controls pass, preserving all required test arguments. The real release command, full coverage suite, production build and seven browser cases pass. T008 closes only its written browser verification scope. The current ratchet still fails on a pre-existing component-count violation and score floor. Native transition timing, repeated visual acceptance and the physical-phone matrix remain unresolved. Current evidence: `output/android-103ms/finish-plan-20260906/receipt.json`; earlier receipts retain their original source/APK scope.

The 2026-09-07 UTC diagnostic continuation changes ordinary IME acceptance from UNVERIFIED to FAIL: real Gboard resize on the unchanged APK leaves clipped or missing native controls under Arabic/RTL, Huge text and reduced motion. The rejected rendering experiments produced no production fix. A newer WebView comparison remains UNVERIFIED because its diagnostic build cannot render the Orb; the read-only test instance was discarded, all seven original AVD hashes match and the original emulator/provider/settings were restored. Main-task completion remains 15 of 19. Latest evidence: `output/android-103ms/notify-completion-20260906/receipt.json`.
## Technical Context
The full suite now records 10,240 passes, no failures, 23 skips and seven existing unfinished tests. Two new 90-second traces of the same independently identified APK each match 5,385 frames with no gaps above 103 ms; maxima are 66.763583 and 66.343750 ms. They do not erase the earlier same-APK maximum of 258.622958 ms or establish input-to-final-presentation latency. Minimal theme observation still measures 154–190.9 ms across six activations. The exact APK retains its decoded full-route video and settled English/ar/he Huge geometry evidence. Ordinary IME, full visual/accessibility, physical-phone and other native-platform acceptance remain open.

React 18, TypeScript, Vite, Capacitor 8; existing dependencies only. IndexedDB remains local truth; no storage/auth/sync changes. Vitest/Testing Library cover ownership and cancellation; Playwright production-build checks cover CSS/route integration; APK identity, UIAutomator actions, separate Perfetto and video captures cover Android. Five V2 destinations. 103 ms applies to prepared finite interactions, not network availability or breathing cycles.
## Constitution Check
Fresh status: PROPOSED, ratified=false, blocking_authority=false. No proposal-only remediation is authorized. Binding authority is the approved user design and AGENTS/runtime/accessibility/data contracts. Pre/post-design review preserves these boundaries.
## Project Structure
DrawerV2.tsx owns retained panel, focus trap and body scroll. useNavigationV2.ts owns intent, history and delayed commit. NavV2Orchestrator.tsx and navV2RouteLoaders.ts own intent preloading; NavV2RouteStatus.tsx owns honest loading. src/index.css changes stay within Android navigation. Canonical orb code is excluded.
.husky/pre-commit will call the existing project typecheck. Android evidence remains in scripts/android-motion and is linked through existing completion/context checks.
The exact tasks/write set are in [implementation steps](../../docs/superpowers/plans/2026-09-04-android-interaction-budget.md).
## Phases
1. Fresh focused baseline at 939daa7e; reduced-motion RED/GREEN and interruption tests.
2. Android-mode browser timing baseline; finite drawer/control durations and necessary paint boundaries.
3. Actual type-error negative control, independent installed-hash evidence, safe recording and source-linked instructions.
4. Full checks, exact APK runtime/visual/frame analysis, independent review and convergence.
## Platform Matrix
| Target/domain | Impact and proof |
|---|---|
| Web/Vite | Shared reduced-motion/cancellation; production Chromium regression |
| Installed PWA | Shared navigation; standalone/offline runtime UNVERIFIED |
| Android/Capacitor | Primary target; exact physical-phone 103 ms UNVERIFIED until run |
| iOS/WKWebView | Shared ownership; native runtime UNVERIFIED |
| Desktop/Tauri | Keyboard/shared navigation; native runtime UNVERIFIED |
| Store/Release | No publishing; release UNVERIFIED |
| Accessibility | Normal/reduced, ar/he, large text, Back/IME, safe areas |
| Performance | Separate response/readiness/final-frame percentiles/max and frame deadlines |
| Security And Privacy | No private records, credentials or network writes in fixtures |
| Testing | RED/GREEN, real type error, mismatched evidence, repeated runtime |
| Operations | Actual exit codes, timestamps/hash identity; scoped rollback |
## Risks And Rollback
Normal exit retains the surface until compositor completion; do not shorten the watchdog to manufacture a metric. Reject changes causing missing controls, focus capture, jitter or canonical visual degradation. Cold/error readiness stays explicit. Verify signer compatibility and preserve installed data before replacement. Revert only the scoped lane patch/commit, never reset the source checkout.
## Current Evidence
The pre-edit locked lane doctor was GO at 939daa7e. That baseline retained a 1000 ms exit fallback, 300 ms panel transition and two double-rAF boundaries. Current implementation and command receipts are tracked in tasks.md and output/android-103ms. Prior audit measurements belong to their original APK/source identities; no baseline or browser result establishes current physical-phone acceptance.

## Native Floating-Keyboard Repair — 2026-09-08

The current SafeArea 8.0.1 listener selects `ime.bottom` from keyboard visibility, although a floating IME can be visible with bottom=0. The fallback branch then loses navigation-bar padding. The 140–143 passthrough workaround also suppresses bottom CSS insets for that non-overlapping IME. Keep the existing owner: floor fallback bottom padding at system-bar bottom, and apply the passthrough keyboard workaround only to a visible IME with nonzero bottom overlap. Preserve docked behavior and the 144 compatibility branch.

Write set: `android/app/src/androidTest/java/com/zenflow/app/SafeAreaKeyboardInsetsTest.java`, `patches/@capacitor-community+safe-area+8.0.1.patch`, its corresponding installed Java file and this feature's evidence. No MainActivity, CSS, version, lockfile, renderer or data change. First dispatch isolated WindowInsets through the actual registered native listener on DecorView and record RED; apply the patch only afterward, then rerun the same test and NativeBackdropInsetsTest. Patch replay must reproduce the installed Java source exactly. Rebuild and verify the benchmark APK and both independent installed identities before actual UI checks.

| Target/domain | This repair's impact and required evidence |
|---|---|
| Web/Vite | N/A: Android-only Java patch; shared source must remain byte-identical. |
| Installed PWA | N/A: no service-worker or web-code change; full-goal PWA proof remains UNVERIFIED. |
| Android/Capacitor | Native safe-area owner; emulator floating/docked/Back/RTL/Huge/safe-area proof required. Other real WebView providers remain UNVERIFIED. |
| iOS/WKWebView | N/A: no iOS or shared-code changes; native full-goal runtime remains UNVERIFIED. |
| Desktop/Tauri | N/A: no packaged desktop changes; full-goal runtime remains UNVERIFIED. |
| Store/Release | No publication from this cycle; release remains UNVERIFIED. |
| Accessibility | Preserve 44 px controls, ar/he directionality, Huge text, Back and top/side system-bar clearance. |
| Performance | No renderer change; geometry checks and encoded-video counts cannot close the 103 ms gate. |
| Security And Privacy | No record or credential reads/writes; fixture insets exist only in androidTest, not the production bundle. |
| Testing | Native RED/GREEN, docked/hidden/landscape/version fallback coverage, patch replay and scoped scanner. |
| Operations | Preserve installed data and keyboard settings; separate actual-provider proof from isolated version tests. |

Rollback: inverse only this new dependency patch, rebuild or reinstall the retained pre-fix benchmark APK `bdc0c59259383836389c5c38d0dc547215df1f99e33af295386eceae4ff3336e` with data retained. Reject new double insets, docked clipping, missing controls or changed backgrounds. Existing other defects and earlier acceptance tasks stay open. The state/data model is unchanged: hidden/floating/docked IME geometry enters the single native inset owner; Back returns ownership to system bars.

## Confirmed Back Teardown Race — 2026-09-08

Scope is FR-003, not a new feature or a restart of the historical plan. Production `Bridge.executeOnMainThread` posts to Android's Handler even when called on the main thread. AndroidBackPlugin checks its callback before posting and then dereferences it after destruction can clear it. Move the callback-existence check into that same queued main-thread operation, before navigation-state mutation; retain input validation and the existing rejection/result contract.

Write set: `android/app/src/main/java/com/zenflow/app/AndroidBackPlugin.java`, `android/app/src/androidTest/java/com/zenflow/app/AndroidBackLifecycleTest.java` and this feature's scoped evidence. The test uses the production plugin class, the actual Activity Bridge and real main queue, with a test-owned plugin instance to isolate asynchronous JavaScript updates. Only the reply boundary is captured; no records are manufactured. First run live/invalid/already-destroyed controls and the deterministic queued-update/destroy reproduction against unchanged production. After the expected crash RED, apply the minimal check relocation and rerun identical tests, AndroidBackNavigationState tests, neighboring native inset tests and source security checks. Rebuild and independently identify benchmark/installed bytes, preserve all web assets, and exercise native Back and Home/resume.

Platform/domain matrix: Android is directly affected; Web/Vite, installed PWA, iOS/WKWebView and Desktop/Tauri implementation are N/A for this Java-only change and their whole-goal runtime remains UNVERIFIED. Back accessibility/lifecycle safety requires native regression and real gestures; this cycle makes no new RTL layout, artistic or103ms claim. Store/release/main integration remains pending. Security/privacy: no journal, storage, auth, permission or network contract changes; only Boolean navigation ownership and revision are tested. Operations: preserve emulator preferences, data, original test package and the current safe-area benchmark `6a4b161dea3d8bfbeda7321620e98b9bc432006497095f62ec3ef038ac23d853`; inverse only this Back patch for rollback. Reject a hanging call, stale state advance, loss of live Back ownership, new API or extra callback owner in production.

Spec/clarify/plan/checklist/tasks/analyze review: the causal stack, production queue implementation and existing FR-003 define the behavior without a product question. All four native cases map to that requirement; native outcome, state mutation, valid control, unchanged data/API and rollback criteria are covered. Constitution remains PROPOSED and nonblocking; extensions stay disabled. No independent reviewer or platform outcome is inferred from this inline analysis.

## Attributed IME Endpoint Conflict — 2026-09-08

**Outcome21:07UTC: candidate rejected and source/patch restored byte-for-byte.** Four native regressions plus12 neighbors passed, but the same real editor video has four0.500–0.617s blank episodes versus0.108–0.133s in the unchanged baseline. The candidate-only test is archived with its APK/source under output, not retained as a failing active product test. The mechanism below is historical, not the next implementation plan. See `IME_INSETS_ANIMATION_EVIDENCE.md` in the current diagnostic packet. Continue by isolating native surface resizing from visible CSS geometry; do not re-add the rejected callback or infer a full-height layout migration is approved.

This is the next reproduced cause in FR-016/FR-018, not a restart of the historical plan. Android dispatches final insets between prepare and start; SafeArea applies them immediately. The rejected diagnostic recursively dispatched animated insets through that same static path, producing alternating63/883/205 padding. Keep animation state inside SafeArea instead: save the latest static target, defer its padding while IME animations are active, apply actual progress overlap directly, and settle the latest target after the last animation ends. Register only on Android31+; retain older static behavior because the official Android30 cancellation contract cannot guarantee end notification.

Write set: the existing SafeArea8.0.1 patch and corresponding Java source, `SafeAreaImeAnimationTest.java`, and scoped evidence. No CSS, MainActivity, dependencies/version/lockfile, assets, keyframe durations, keyboard controller, permissions, storage, security or auth change. RED at20:54 has four expected failures on original source808d5352; unchanged test97fe19fa must run GREEN before real-video acceptance. Existing native floating/version/landscape/backdrop/Back tests, exact patch replay and scoped Snyk follow. Then repeat the same baseline-only instrumentation and editor window recording, with no test callback, CDP, build or scan during capture; verify hashes before/after and inspect all suspicious frames. Rebuild benchmark and repeat real settings/Back/RTL/Huge/floating/docked/resume checks only if the candidate improves pixels.

Platform/domain matrix: Android31+ inset ownership is affected; Android26–30 is unchanged and runtime UNVERIFIED. Web/Vite, installed PWA, iOS/WKWebView and Desktop/Tauri implementation are N/A for this Java-only cycle; their full-goal proof stays open. Accessibility requires safe controls, RTL/Huge and normal/reduced-motion review. Performance includes possible per-frame WebView relayout cost and separate103ms presentation evidence; reject a slower or more visibly broken candidate. Security/privacy keeps fixtures in androidTest and collects geometry only. Store/release/main integration is pending. Operations preserve installed data, original test APK and public/keyboard settings; rollback uses the exact prior Java and patch in `ime-insets-before-*`, never removing the earlier floating-IME repair.

Spec/clarify/plan/checklist/tasks/analyze: FR-018 maps to T028 and all four tests; static target, progress, cancellation, interruption, legacy fallback, visual rejection, source identity and rollback are explicit. No critical ambiguity or constitution-derived remediation; the constitution is freshly PROPOSED/unratified. Native GREEN, inline review and geometry are not proof of continuous motion, human craft or complete user-goal acceptance.
