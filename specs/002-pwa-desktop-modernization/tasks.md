# Tasks: Installed PWA Modernization for macOS and Windows

**Feature**: `002-pwa-desktop-modernization`  
**Execution rule**: Test-first; one product decision and rollback per batch; no push/deploy/Store action.

## Phase 1 — Preflight and Spec Kit

- [x] T001 Establish clean locked `codex/pwa-desktop-modernization` worktree at `13ca51a80d23220574deba762851fe5a32372e46` and run edit doctor.
- [x] T002 Run Free RAG, read active architecture/PWA/Spec Kit/test-first/data/translation/logo/release policies, and record constitution status.
- [x] T003 Complete `spec.md`, clarification decisions, `research.md`, `data-model.md`, evidence schema, `plan.md`, and requirements checklist.
- [x] T004 Record ten-role routing dispositions, invoke the required read-only specialist/sentinel/QA routes, incorporate evidence-backed findings, and retain unavailable independent/isolation proof as `UNVERIFIED`.

## Phase 2 — RED Characterization

- [x] T005 [P] Extend `scripts/__tests__/public-webmanifest-contract.test.ts` with failing built/tracked contract expectations: no orientation lock, no stale development layout parameter, in-scope V2 shortcuts, 1024px maskable icon.
- [x] T006 [P] Extend `src/hooks/__tests__/usePwaInstall.test.ts` with failing cases for Safari standalone, macOS manual capability, malformed prompt, and prompt clearing after dismissal/error.
- [x] T007 [P] Add `e2e/pwa-desktop-lifecycle.spec.ts` with failing production-equivalent cases for JSON locale parsing, RTL/title, honest offline copy, desktop shortcut layout, resize/reflow, keyboard action, and worker fallback.
- [ ] T008 Core pre-edit chronology is `UNVERIFIED`: the retained exact base replay is retrospective. Keep it as base characterization; only later review-remediation receipts qualify as chronological RED→GREEN.
- [x] T009 Create a fresh ignored `.preflight-token` containing task-specific test-first and selected-skill evidence after RED is confirmed.

## Phase 3 — Install and Manifest Batch

- [x] T010 Implement non-native page-lifetime prompt capture in `src/lib/pwaInstallPrompt.ts`/`src/main.tsx` plus pure installed/platform/browser classification and one-shot handling in `src/hooks/usePwaInstall.ts`, without persisting capability state.
- [x] T011 Add macOS Safari safe-install guidance to `V2SettingsAboutPanel.tsx`, including separate-state/re-auth disclosure, preservation/verification steps, and no universal sync/backup claim.
- [x] T012 Add natural copy keys to all eight locale files; preserve placeholders, parity, RTL, reflow, and implementation-term restrictions.
- [x] T013 Remove `orientation` and `navLayout=phone` from Vite/generator/tracked manifest contracts while keeping `nav=v2`, scope, and identity.
- [x] T014 Generate `pwa-maskable-1024.png` from the frozen canonical leaf, update the shared install-icon revision/checker/include lists, and sync only intended public/docs generated assets.
- [x] T015 Run retained candidate contracts GREEN plus Settings tests, manifest build inspection, i18n checks, logo checks, proof sheet, and bounded visual inspection; do not relabel the retrospective base replay as chronological TDD. Real OS rendering and artistic acceptance remain `UNVERIFIED`.
- [x] T016 Inspect the batch diff and record rollback: revert source/generator/copy changes, regenerate assets, and rerun T005/T006.

## Phase 4 — Offline and Service-Worker Batch

- [x] T017 Update `public/offline.html` and its canonical docs copy to parse stored locale safely, localize title/direction, use precise degraded copy, support 200%/forced-colors/reduced-motion, and keep retry accessible.
- [x] T018 Update `src/sw.ts` to prefer app shell, then precached offline document, then explicit error; do not cache or replay product POST requests.
- [x] T019 Remove unused `CLEAR_CACHES`, `SKIP_WAITING`, and `REGISTER_SYNC` message handling; namespace new runtime caches; retain trusted used messages and direct client Background Sync registration while documenting its open-client limit.
- [x] T020 Deduplicate runtime-audio warm paths while preserving first-use order, cache name, range behavior, connection/save-data gates, and failure logging.
- [x] T021 Run T007 GREEN plus existing diary offline, PWA audio range, service-worker message, offline queue, version check, and durable reload tests.
- [x] T022 Inspect the batch diff and record rollback without cache/user-data deletion.

## Phase 5 — Desktop Runtime and Cross-Platform Proof

- [ ] T023 Rebuild the frozen production Web/PWA artifact and validate source/public/docs/dist manifest equivalence, worker registration, precache inventory, icon hashes, and base path.
- [ ] T024 Run Chromium production PWA scenarios for install capability, shortcuts, offline route, fallback, update, and narrow/medium/wide resize with console/network/overflow/focus facts.
- [ ] T025 Run available WebKit coverage and attempt a real macOS Safari/Add to Dock inspection only if safe local automation is available; otherwise record exact `UNVERIFIED` reason.
- [ ] T026 Validate the implemented automated RTL/reflow/reduced-motion/forced-colors scope and keep unrun 200% Settings, human keyboard/AT, and theme rows `UNVERIFIED`.
- [ ] T027 Run all four configured browser projects, Android/iOS PWA-pruning boundaries, canonical orb check, and Desktop/Tauri updater/executable contract.
- [ ] T028 Record real Windows Edge/install/Start/taskbar/update/offline/enterprise-policy and real mobile browser-installed/safe-area rows as `UNVERIFIED`; do not map macOS engine/viewport simulation to those platform IDs.

## Phase 6 — Security, Data, Performance, and Broad Gates

- [ ] T029 Rerun frozen TypeScript and focused Vitest separately; then `check:all` and all affected test suites with exact counts.
- [ ] T030 Rerun translation parity/deep/quality checks and automated RTL artifact scope; keep native-speaker acceptance `UNVERIFIED`.
- [ ] T031 Run production-data-integrity diff, final production build, then bundle integrity sequentially; confirm no mock business records or user data.
- [ ] T032 Run Snyk Code or documented CLI fallback on changed first-party code and `codex-security-suite.sh --profile auto`; triage changed-code findings without weakening gates.
- [ ] T033 Run PWA/icon/release artifact checks, Chrome performance smoke, and compare startup/precache/bundle receipts to current budgets.
- [ ] T034 Run no-AI-template, best-practices, agent-context/orchestra, doc-count/constitution applicability, sync contract, and `ci:preflight`; reproduce inherited failures on clean base before attribution.
- [ ] T035 Regenerate/validate `pwa-evidence.schema.json` records for the frozen hash and confirm artifacts contain no tokens, PII, journal, mood, habit, account, device, or contact data.

## Phase 7 — Analyze, Converge, and Handoff

- [ ] T036 Rerun cross-artifact analysis after the frozen receipts; map every ER/IR/FR/SC to current evidence IDs or exact blocker.
- [ ] T037 Run Role 8 QA evidence review and Role 10 hash-bound closure pass; resolve hard STOPs or retain the goal as incomplete.
- [ ] T038 Rerun the local rollback drill against every frozen final behavior batch, bind exact patch hashes/commands/exits/candidate hash, and prove clean reverse state.
- [ ] T039 Read the complete final diff/status, inspect generated assets/proof sheet, and confirm no unrelated work or stale token artifact is tracked.
- [ ] T040 Regenerate `convergence.md` from current evidence with platform/domain matrix, exact command results, limitations, rollback, release `GO/STOP`, and no overclaim.
- [ ] T041 Mark the goal complete only when all authorized local requirements are met and no unexplained P0/P1 failure remains; keep external deployment/Windows/Store/human rows explicitly `UNVERIFIED`.

## Dependencies

- T005-T009 must complete before T010-T020 production/config edits.
- T010-T016 form one reversible install/manifest batch.
- T017-T022 form one reversible offline/worker batch and start only after the first batch is green.
- T023-T028 require both implementation batches.
- T031 runs build before bundle integrity and never concurrently with another build mutation.
- T036-T041 require all available verification receipts; unavailable external environments do not become local `PASS`.

## Parallel Safety

Tasks marked `[P]` may be authored independently only before production edits and with non-overlapping file ownership. Generated icon assets, Vite build output, production-data bundle scans, final diff review, and convergence are sequential.
