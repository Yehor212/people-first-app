# Research: Integration Compatibility

The official [React 19 upgrade guide](https://react.dev/blog/2024/04/25/react-19-upgrade-guide) and [common DOM props](https://react.dev/reference/react-dom/components/common) were checked on 2026-09-09. Runtime/types must be paired; refs require an argument, element props require narrowing and JSX namespace references are scoped. Local RED proves the actual defect: four existing tests lose the inert attribute with string props. Preserve null/undefined lifecycles and pass boolean inert; imperative DOM attribute APIs remain unchanged.

The configured circular check includes async imports and reports twelve cycles. The db/setting-policy/tracker cycle only needs a pure constant leaf. Two automation and seven journal cycles reach the producer through the delta consumer. Source inspection of eventSync.ts and journalSecurityMigration.ts shows that moving journal recovery would disturb substantially more owner/revision-bound helper logic. Move the cohesive producer instead, preserving public function identity and one cache. The final two stats cycles are self-barrel imports and need direct leaf imports only.

Local evidence sources: src/storage/eventSync.ts; src/storage/deletionTracker.ts; src/storage/sync/settingSyncPolicy.ts; src/features/automation/automationPreferences.ts; src/storage/__tests__/eventSync.test.ts and eventSyncAutomationTransaction.test.ts; existing sync/journal regressions; scripts/check-sync-contract.cjs. Executed full-suite RED receipt: ignored output/main-convergence-20260909.hV5KFc/react19-baseline.json. Local receipts are not public/device proof.

Rejected alternatives: downgrading only React discards incoming dependency intent; casting string inert retains the demonstrated interaction defect; excluding async imports conceals cycles; moving diary recovery increases risk unnecessarily; duplicated identity helpers split the cache across logout. Existing producer invariants must follow their implementation rather than disappear.

## Cold Diary Editor Delivery, 15:23 Owner Approval

The strict production offline trace reaches Diary but fails five requests: editor, its text-editor vendor, two widgets and FloatingMediaLayer. Their current emitted size is 318558 bytes. `JournalModule.tsx` already lazy-loads the editor on original main; `vite.config.ts` precaches the shell while `src/sw.ts` caches remaining static assets only after use. No online editor visit is added to the test.

The official [Vite plugin API](https://vite.dev/guide/api-plugin.html) documents emitted chunk CSS/asset metadata; installed Rolldown output types expose moduleIds, facadeModuleId and imports. The official [Workbox build reference](https://developer.chrome.com/docs/workbox/modules/workbox-build) documents manifest transforms. Installed Workbox applies custom transforms after its maximum-size filter and calculates final counts/sizes afterward: enforce the existing 3 MiB file cap inside the collector and the owner-approved 640 KiB added-payload cap after URL deduplication. Build-time SHA-256 revisions are computed from the emitted code/asset bytes, not source paths or cached old filenames. The earlier 512 KiB preliminary cap rejected the complete 566129-byte editor/list/image closure; direct approval at 16:08 UTC resolved that measured budget decision.

Rejected: all-JS globs grow unrelated installation work; hand-listed five names miss future transitive edges; eager runtime imports disturb startup; parsing minified import strings loses emitted CSS metadata; additionalManifestEntries runs after transforms and makes complete-shell deduplication less direct. The selected collector uses exact build metadata and the existing injection pipeline, with no new dependency or service-worker runtime code.

Actual graph inspection showed that the full closure adds 1040386 bytes before final preload rewrites, including shared dependencies of the already executed initial entry. The cold delta is 345090 bytes across sixteen files; unrelated optional dynamic features remain excluded. Preserve the existing controlled online boot and visited-route cache contract by subtracting its metadata-derived static graph rather than precaching the whole boot subsystem. New tests require known boot roots, complete boot edges and a non-eager editor, alongside the unchanged added-size limit. Vite's late generateBundle hook changes editor preload-list bytes; writeBundle is the verified lifecycle boundary before Workbox's closeBundle injection. Final file-hash verification is required in addition to graph unit tests.
