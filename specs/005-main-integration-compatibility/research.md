# Research: Integration Compatibility

The official [React 19 upgrade guide](https://react.dev/blog/2024/04/25/react-19-upgrade-guide) and [common DOM props](https://react.dev/reference/react-dom/components/common) were checked on 2026-09-09. Runtime/types must be paired; refs require an argument, element props require narrowing and JSX namespace references are scoped. Local RED proves the actual defect: four existing tests lose the inert attribute with string props. Preserve null/undefined lifecycles and pass boolean inert; imperative DOM attribute APIs remain unchanged.

The configured circular check includes async imports and reports twelve cycles. The db/setting-policy/tracker cycle only needs a pure constant leaf. Two automation and seven journal cycles reach the producer through the delta consumer. Source inspection of eventSync.ts and journalSecurityMigration.ts shows that moving journal recovery would disturb substantially more owner/revision-bound helper logic. Move the cohesive producer instead, preserving public function identity and one cache. The final two stats cycles are self-barrel imports and need direct leaf imports only.

Local evidence sources: src/storage/eventSync.ts; src/storage/deletionTracker.ts; src/storage/sync/settingSyncPolicy.ts; src/features/automation/automationPreferences.ts; src/storage/__tests__/eventSync.test.ts and eventSyncAutomationTransaction.test.ts; existing sync/journal regressions; scripts/check-sync-contract.cjs. Executed full-suite RED receipt: ignored output/main-convergence-20260909.hV5KFc/react19-baseline.json. Local receipts are not public/device proof.

Rejected alternatives: downgrading only React discards incoming dependency intent; casting string inert retains the demonstrated interaction defect; excluding async imports conceals cycles; moving diary recovery increases risk unnecessarily; duplicated identity helpers split the cache across logout. Existing producer invariants must follow their implementation rather than disappear.
