# Quickstart: Verification Order

Run from the locked feature worktree only.

## 1. Confirm target and inputs

1. Run workspace doctor in edit mode for Codex.
2. Confirm branch codex/animation-quality-remediation-20260829 and clean ownership.
3. Recompute both audit hashes and stop if either differs from the specification.

## 2. RED before production edits

Run the focused route-sanitization negative tests, Schedule reduced-motion contract, and Android empty-ID policy test before changing production code. Record the expected failures in the fresh preflight token. A missing test or a test that unexpectedly passes is not RED evidence.

## 3. Implement one batch at a time

Order:

1. route privacy sanitizer and legacy snapshot rewrite;
2. Schedule gate-only propagation;
3. Android debug AdMob fail-fast.

After each batch, rerun the identical focused command and its adjacent test suite before proceeding.

## 4. Blast-radius checks

Run:

- typecheck and lint;
- focused observability and Schedule tests;
- Android configuration/build contracts;
- i18n and RTL checks;
- canonical-orb contract;
- architecture and constitution count checks;
- production-data integrity diff/full and post-build bundle checks;
- narrow security suite, Snyk fallback if available, and npm audit;
- production build.

Do not weaken a baseline, policy, assertion, or scanner to obtain green output.

## 5. Runtime and visual evidence

- Use real empty/unavailable application state; do not seed plausible events or user history.
- Capture full-motion control and reduced-motion state at the same route, viewport, locale, theme, and lifecycle.
- Verify that full-motion constants and composition are unchanged and reduced mode remains static for the complete observation window.
- Test ar and he directionality, safe areas, desktop width, installed PWA, Android, iOS, and Desktop only where the exact runtime is available.
- If auth or platform access blocks a faithful capture, mark the row UNVERIFIED.

## 6. Closure

Inspect git diff and git status, run visual-integrity review, independent QA, and Role 10 hash-bound Pass B. Report technical, visual runtime, artistic/craft, motion, model, plan, five-platform, security/privacy, testing, operations, and release status separately. Do not commit, push, deploy, publish, or change versions.

