# AGENT_CHANGE_NOTICE — Wave 3 Release Capability

- **Risk**: L4 release/build configuration; production motion admission across Web/PWA, Android, iOS, and Tauri.
- **Trigger**: Epic 002 FR-025 and FR-028–FR-034 require one exact-commit, privacy-safe, fail-closed capability receipt. The current Vite config directly accepts `VITE_ENABLE_JOURNAL_SAVE_CEREMONY`, while Tauri has a separate handwritten Vite command.
- **Current evidence**: `npx vitest run --configLoader runner scripts/__tests__/feature-capability-receipt.test.ts scripts/__tests__/feature-capability-release-contract.test.ts` exited 1 with 7 expected failures and 8 behavior controls skipped behind the missing implementation. The failures identify the absent builder/policy/Tauri path/workflow validators and the direct environment admission path.
- **Proposed write set**: `config/feature-capabilities.json`; `scripts/feature-capability-build.cjs`; `scripts/check-feature-capability-receipt.cjs`; `scripts/run-shared-dist-build.mjs`; `scripts/__tests__/feature-capability-*.test.ts`; `scripts/__tests__/shared-dist-build.test.mjs`; `vite.config.ts`; `src/lib/env.ts`; `package.json`; `src-tauri/tauri.conf.json`; `.github/workflows/deploy.yml`; `.github/workflows/desktop-release.yml`; Epic 002 evidence/tasks/traceability.
- **Platform/domain impact**: production build configuration and artifact metadata for GitHub Pages/PWA, Capacitor Android, Capacitor iOS, and Tauri Desktop. No product data, persistence, auth, sync, or production service write is authorized by this change.
- **Safety boundary**: schema v1 is structurally non-enabling. Even `requested=true`, an inactive kill switch, and six `pass` strings must produce `journalSaveCeremony=false`. Artistic/craft and explicit user visual approval remain `UNVERIFIED`, so the ceremony stays out of every production bundle.
- **Rollback**: keep `requested=false` and `killSwitch=true`, or revert the release-wiring commit and rebuild. No data migration or user-state rewrite is involved.
- **Verification**: rerun the exact RED command GREEN; shared-dist parser/plan tests; TypeScript; deterministic four-platform receipt-set validation; all four available production builds; bundle no-enable inspection; workflow contracts; sequential production-data-integrity bundle checks; record unavailable native/runtime/human gates as `UNVERIFIED`.
- **Verdict**: `GO` for non-enabling receipt and release parity only. `STOP` for production animation enablement.
