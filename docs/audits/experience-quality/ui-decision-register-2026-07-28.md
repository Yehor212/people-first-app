# ZenFlow UI decision register — 2026-07-28

Subject HEAD: `e5016f156497a9d3e55578b773294bf56adce58e`

This register records product/audit decisions that changed implementation or
bounded what was intentionally not changed. It does not turn an inference or
missing platform proof into acceptance.

| ID | Decision | Evidence and reason | Rejected alternative / rejection criterion | Rollback | Status |
| --- | --- | --- | --- | --- | --- |
| DEC-01 | Keep Style Dictionary and the existing generated-token path as the canonical foundation. | `package.json`, generated token outputs, runtime theme variables, the UI-system contract, and token inventory show an active pipeline. Replacing it would broaden blast radius without evidence. | Reject a parallel token system or screen-specific raw values. Reconsider only if the current generator cannot express a required semantic role with tested migration. | Revert the added semantic aliases/guards while retaining the pre-existing generator. | IMPLEMENTED / VERIFIED locally |
| DEC-02 | Express Settings through ZenFlow's own grouped-list grammar, not a Finch/iOS/Material clone. | Settings source/runtime showed nested containment and inconsistent row anatomy; the reference image supplied only grouped-list principles. ZenFlow tokens, Lucide utility icons, leaf/orb brand assets, and platform deviations remain canonical. | Reject copied colors, banner shape, icons, copy, or pixel composition. Reject flattening that removes semantics, focus, warning, or recovery containment. | Revert primitive migration as one presentation wave; no data rollback. | IMPLEMENTED / bounded Chrome evidence |
| DEC-03 | Preserve auth, backup, deletion, profile-owner, sync, and IndexedDB semantics while repairing their UI publication. | Account tests and source traces distinguish owner-bound async outcomes and irreversible actions. The audit had no authority or evidence for a data/schema redesign. | Reject fake success, owner result leakage, production fixtures, schema migration, or remote mutation. | Revert Account/Profile UI guards and paired tests; no stored-data rollback. | IMPLEMENTED / remote services UNVERIFIED |
| DEC-04 | Make offline/update failure explicit instead of fabricating content or silently failing. | Baseline offline locale parsing and early chunk-error dispatch had reproducible failure paths. The fixes normalize existing device-local preference and buffer one ephemeral incident. | Reject synthetic content, automatic reload before explanation, persisted incident data, or raw provider error disclosure. | Restore prior static bootstrap/listener behavior; no data migration. | IMPLEMENTED / installed-PWA lifecycle UNVERIFIED |
| DEC-05 | Model external calendar readiness/error as state; do not present unavailable data as authoritative empty. | UI-22 source/component RED/GREEN proof covers disabled/loading/ready/error/retry and privacy-safe error classification. | Reject fake events, raw provider exceptions, unbounded automatic retry, or browser-runtime PASS without an isolated provider check. | Restore event-array-only presentation; no OAuth/event migration. | IMPLEMENTED in source; live/browser transition UNVERIFIED |
| DEC-06 | Remove decorative containment, particles, glow, repeated motion, and emoji only where they competed with the user job; preserve semantic event color and canonical expressive assets. | Controlled before/after comparisons and component contracts for Onboarding, Focus, Hyperfocus, Planning, Schedule, and Add Event identify one task hierarchy and retained data cues. | Reject “flat at any cost,” generic generated wellness art, copied competitor assets, and removal of event identity. | Revert each bounded presentation wave without changing timer/event/onboarding payloads. | IMPLEMENTED / expert craft GO for seven comparisons |
| DEC-07 | Keep Planning's existing explicitly dark main surface even when the outer theme is Paper. | Fresh computed styles prove an opaque `rgb(8,18,17)` main with 16.56:1 label and 10.60:1 icon contrast. Independent critic retracted the hypothesis that the transparent nested scope composited on Paper. | Reject another wrapper card/background or a theme rewrite based on a crop artifact. Reopen only with fresh named-platform compositing failure. | N/A; no code change was made. | PRESERVED / Chrome shared-source proof |
| DEC-08 | Treat Task 15 component-crop metadata as an evidence-quality gap, not a product defect. | `AF-T15-06` omits ancestor compositing and the receipt lacks locator/clip/dimensions. Source/runtime counterevidence disproved the inferred contrast failure. | Reject using an undeclared crop as whole-page proof. Future receipts must record capture kind, locator, clip, dimensions, and viewport mapping. | N/A. | OPEN P3 evidence improvement |
| DEC-09 | Downgrade immutable-baseline build identity instead of substituting a later rebuild. | Baseline PNG bytes and renderer metadata remain, but the exact bound production manifest bytes were overwritten. A fresh detached-HEAD build produced a different artifact hash and cannot authenticate the old capture. | Reject relabeling a later build as the captured artifact or leaving a stale `VERIFIED` receipt. | Restore `VERIFIED` only if the exact original manifest bytes and matching canonical hash are recovered. | UNVERIFIED build identity; screenshot bytes retained |
| DEC-10 | Apply bounded Habits Back/focus and challenge-link destination remediation without claiming native parity. | UI-9 and UI-10 are `VERIFIED/FIXED` for their local source/component contracts after targeted RED/GREEN proof: the action sheet now has one repository Back owner and deterministic focus handling, while valid challenge intents reach one truthful no-write V2 dialog. Native Back and app-link delivery remain `UNVERIFIED`; UI-29 separately records the broader pre-existing URL-admission boundary. | Reject remounting legacy challenge write paths, changing invite compatibility inside UI-10, or promoting local JSDOM/source proof to device/platform PASS. Reopen either fix if the bounded regression tests fail; close native cells only with fresh device/runtime evidence. | Revert each finding's implementation and paired tests as one unit; no user-data rollback is required. | IMPLEMENTED / bounded local proof; native runtime UNVERIFIED |
| DEC-11 | Keep browser, PWA, Android, iOS, Desktop, AT, and human evidence as separate cells. | Shared React/CSS and a successful build do not prove WebView/native lifecycle, permissions, system UI, packaged desktop behavior, assistive-technology speech, or user acceptance. | Reject “all platforms” from a Web screenshot or build. | N/A. | GOVERNANCE ACTIVE |
| DEC-12 | Serialize artifact-sensitive builds and integrity scans across agents. | The repository manifest guard detects concurrent dist mutation; Task 11 and Task 16 share one worktree. Coordinator ordering prevents false `bundle artifact inventory changed` failures. | Reject concurrent build/bundle validation in the same lane. | Stop the later process and rerun from a clean artifact state. | ACTIVE |
| DEC-13 | A dedicated same-origin `version.json` request routed through Workbox `NetworkOnly` is the only local connectivity evidence allowed to override a stale offline shell state. | XQ-18 reproduced the authority gap: the former favicon probe was an app asset that the service worker could satisfy from cache. The shared probe constant, exact scope matcher, no-store request, bounded timeout, and abort-owned queue evidence now form one contract. | Reject a generic app asset, cross-origin endpoint, cached response, unbounded wait, or evidence token without cancellation ownership. | Revert the client probe, service-worker route, and queue evidence type together; never restore cached-asset authority independently. | IMPLEMENTED / local production offline-reconnect proof |
| DEC-14 | Primary route and task content must fail open at animation time zero; decorative motion may remain progressive. | XQ-19 retained a pre-fix Tauri screenshot with Habits navigation but an absent main region, plus source/test counterexamples across route and task-critical opacity-zero entrances. The post-fix receipt directly shows essential surfaces at opacity 1 while 21 animations remain at currentTime 0. The exact pre-fix computed-style/AX diagnostic was not retained. | Reject global motion removal, arbitrary recovery timeouts, screen-specific visibility hacks, or any change to routing/data/auth/sync semantics. | Revert only the `initial={false}`/whole-page entrance removals as one presentation wave; retain both static and runtime regression tests and STOP the affected release cell if opacity-zero dependency returns. | IMPLEMENTED / named source owners and rebuilt artifacts; platform trigger remains UNVERIFIED |

## Human decisions not supplied by this audit

- No human research validated preference, perceived calmness, trust,
  “premium” quality, emotional benefit, or cultural acceptance.
- No authenticated product owner accepted an open native/accessibility risk as
  release-ready.
- No legal/store/security approver reviewed release listings, privacy labels,
  signing, or uploaded artifacts.

HUMAN_ACCEPTANCE: UNVERIFIED
