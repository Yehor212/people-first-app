# Deferred Findings Ledger

## 2026-08-20 — T222-R scoped security scan

- Evidence: `/Users/yehor/.codex/security/reports/20260820T040704Z-67939/summary.tsv`.
- Scope decision: no remediation in T222-R. The findings are outside this Ads-OFF provenance delta.
- Dependency scan: Trivy reported existing advisories in `package-lock.json` and `src-tauri/Cargo.lock`; `npm uninstall @capacitor-community/admob` did not add a dependency. `npm uninstall` had already reported four high-severity audit findings before the fresh install.
- Secret scan: Gitleaks reported only redacted matches in files outside the T222-R diff, including test sources and pre-existing generated iOS output. No secret values were read or copied.
- Scanner limitations: Snyk Code returned 23 pre-existing non-blocking findings in unrelated files; Terrascan could not parse pre-existing Playwright capture YAML. Neither is evidence of an Ads-OFF regression.
- Owner follow-up: security/dependency owners should triage the report separately; this task neither waives nor fixes those findings.

## 2026-08-20 — T192-R Android motion baseline follow-ups

- Evidence: `output/android21/motion/t192-r/runtime-go-final/runtime-matrix.json`, `output/android21/motion/t192-r/runtime-go-final/device-runtime-summary.json`, and `/Users/yehor/.codex/security/reports/20260820T160042Z-18618/summary.tsv`.
- Performance finding: the isolated API 36 `arm64-v8a` AVD used headless SwiftShader plus Android `screenrecord`; all 12 accepted current-state rows reported 100% janky frames through `gfxinfo`, and the final task log contained 411 `tile memory limits exceeded` warnings. The captures remained visually complete, but these measurements cannot be promoted to physical-device performance proof. T189 or the later motion implementation task must retest an exact candidate without concurrent screen recording on a representative physical Android device and define an acceptance budget before claiming performance or release GO.
- Motion-quality boundary: T192-R records current Gratitude and Let Go behavior only. Direct review found the normal/reduced state sequences, RTL layout, dismissal, and cleanup usable enough to retain as baselines; it does not grant artistic approval or authorize a redesign. T193–T196 own direction, variants, comparison, and selection.
- Dependency finding: fresh `npm audit --audit-level=high` reports 6 current advisories (4 high, 2 moderate). The ordinary `check:all` dependency compatibility blocker was fixed with version-scoped `brace-expansion` overrides, but advisory remediation is a separate security/release task and was not hidden or auto-applied here.
- Scanner limitations: Gitleaks, TruffleHog, Trivy, Checkov and KICS completed; Snyk Code reported 23 existing results outside the T192-R write set, including one warning in a local HTTP e2e helper; Terrascan returned parse errors on pre-existing Playwright capture YAML and non-IaC paths. These results are neither a security PASS nor a T192-R regression.
- Owner follow-up: preserve the exact hashes in the T192-R receipt, route performance to T189/T193–T196 and dependency/security triage to its own authorized task; logging these rows does not authorize implementation, publication, or a release decision.

## 2026-08-21 — T175-R custom-habit creation persistence candidate

- Stable ID: `T175-DF-ANDROID-CUSTOM-HABIT-PERSISTENCE`.
- Status / severity: `UNVERIFIED_HIGH`. This is not accepted as a confirmed product defect or a T175 completion blocker because the retained packet does not include a row-level submit-to-Dexie transcript for this separate creation flow.
- Candidate evidence: `/Users/yehor/.codex/worktrees/9fad/people-first-app/output/android21/core/t175-r/deferred-finding-candidates.json` SHA-256 `65d16e8968aa00ce5574784e90a5e97637de498a360308224e09acb7fdf94f15`; historical post-submit capture `after-create.png` SHA-256 `a202c4fe4ea4962d4711d03e4c3dc471aec839a9d06661eb569a6b00f47a0104`; historical empty-state capture `habits-empty.png` SHA-256 `1328d9ff783e03375c6d6c9f27e711c4bf26f7c8ffa494e84a6ff6ee16a8c58f`.
- Observation: on a clean API 36 installed release derivative, the accessibility flow `Create habit` → `Create custom habit` → fill name → `Add habit` appeared to close the sheet, after which the task reported zero records in `ZenFlowDB.habits`.
- User/platform risk: a custom habit could appear accepted without durable persistence. The observation is Android/Capacitor-only; Web/Vite, installed PWA, iOS/WKWebView and Desktop/Tauri impact are `UNVERIFIED` because the shared submit path was not independently replayed there.
- Privacy boundary: the task used one isolated synthetic non-business record and reports no retained user data, credentials, account identity, private prose or external write.
- Required verification before promotion: reproduce on the exact future candidate with the Pixel 10 API 36 functional profile; retain accessibility-node actions plus before/after Dexie probes; verify Zustand-to-Dexie ordering, reload/process-death persistence, duplicate-submit handling, offline behavior, eight-locale/RTL/a11y state, and cleanup. A negative control must prove the probe detects an intentionally absent record. Physical-device and cross-platform conclusions remain separate.
- Ownership boundary: route to an authorized habit-creation persistence task when its dependency point is reached. Logging this candidate does not authorize implementation, integration, publication or release, and it does not place the finding in ZenFlow 2.1.0.

## 2026-08-21 — T178-R core-without-monetization follow-ups

- Evidence boundary: accepted T178-R receipt SHA-256 `dfe5718cbd95e17053813700cf9e2f3e905e8b8035c3f09ce19d74ae16871780`; `failures-unverified.json` SHA-256 `6ba43c6a95fe73d50669340aa1191a2b6f1f84a444aa3dbf1e73da8ce92b0dac`. T178-R itself has no remaining in-scope failure. These rows are follow-ups only and do not change `TASK_LOCAL_COMPOSITE_ONLY`, integration, next-build, or release state.
- Stable ID `T178-D02` — `DEFERRED_OUT_OF_SCOPE`: the existing Web E2E harness recorded 14 pass, 1 skip and 5 failures from stale `analytics.init` and account-verification import expectations, while the exact T178 core harness passed and broader Settings/unit contracts passed. Treat this as an unconfirmed harness-maintenance candidate, not a deployed Web defect; QA must reproduce against the current production-equivalent route and retain negative controls before promotion. Installed PWA, Android, iOS and Desktop impact is `UNVERIFIED`.
- Stable ID `T178-D03` — `DEFERRED_OUT_OF_SCOPE`: a fresh dependency postinstall is not reproducible because the local-notifications patch applies but `brace-expansion+5.0.8.patch` does not match installed 5.0.9. Dependency/build ownership must repair and verify the ordinary clean-install graph without a shim or weakened gate; until then clean-install reproducibility is `UNVERIFIED` across all five build targets.
- Stable ID `T178-D04` — `INHERITED_SECURITY_DEBT`: `npm audit` reports 3 high and 2 moderate advisories. Fresh Snyk produced 23 repository-wide findings (22 low, 1 medium), none in the five remediation paths or 58-path T178 composition, then ended with terminal 403. This is neither a blanket security PASS nor a T178-attributable regression; security/dependency owners must triage under separate authority.
- Stable ID `T178-D05` — `MEASUREMENT_ONLY_T189`: the first post-install software-emulator WebView start timed out at `WaitTime=10665ms`; a subsequent force-stop cold launch of the exact retained artifact succeeded at `TotalTime=3446ms`. One inconsistent custom-AVD observation is not optimization evidence. T189 owns repeated before/after startup, frame pacing, memory, thermal/profile and no-quality/content-loss proof; physical Pixel 10/Tensor G5/OEM performance remains `UNVERIFIED`.
- Stable ID `T178-D06` — `RELEASE_BOUNDARY`: the T178 task-local future-update composite reports `versionName 2.0.0`; the immutable submitted Play artifact is ZenFlow 2.1.0, versionCode 35, and excludes T178 by the owner boundary. T178 is not `INTEGRATED`, not `RELEASED`, and its presence in any next build remains `UNVERIFIED` until an owner-approved cumulative lane and exact future artifact exist.
- Ownership boundary: logging T178-D02 through T178-D06 authorizes no implementation, dependency mutation, integration, build publication, Play/Supabase action, or release decision.

## 2026-08-21 — T179-R reachability follow-up

- Evidence boundary: accepted T179-R receipt SHA-256 `94590abfa1eddf9ccdbf1789e0b7a9bc1600e21824f1fced0c9ffe179b23fd6b`; `failures-unverified.json` SHA-256 `f0bf67ab7576c4b4d1fe107666e3f4a29d6ba205f04e2a3b41d2986e3e639471`. T179-R has no remaining in-scope failure and changes no production dependency or application source.
- Stable ID `T179R-CAND-INHERITED-DEPENDENCY-AUDIT-001` — `INHERITED_SECURITY_DEBT`, reconfirming rather than superseding `T178-D04`: fresh `npm audit` reports 3 high and 2 moderate advisories in the inherited dependency graph. The task-local evidence tools initially had six Snyk findings, were repaired within the task packet, and fresh coordinator-scoped Snyk plus no-git Gitleaks then reported zero findings in that exact tools scope. The broader quick security suite remains `UNVERIFIED` because its Git-history Gitleaks mode surfaced 14 inherited candidates outside the task paths; its process exit alone is not a repository security PASS.
- Platform/risk boundary: the inherited dependency graph can affect Web/Vite, installed PWA, Android/Capacitor, iOS/WKWebView and Desktop/Tauri packaging, but exploitability and shipped-artifact reachability are not established by this candidate. T179's Ads-OFF reachability result, the immutable 2.1.0 Play artifact and any future cumulative build remain separate evidence boundaries.
- Required follow-up before promotion: an explicitly authorized dependency/security task must reproduce from a clean ordinary install, preserve the standard postinstall and scanners without shims, attribute each advisory to source and produced artifacts, apply only compatible upgrades/overrides, rerun focused/full tests and all five platform build/runtime gates, and retain rollback plus negative controls. Missing native/store/runtime proof stays `UNVERIFIED`.
- Ownership boundary: logging this stable ID authorizes no package mutation, production fix, integration, build publication, Play/Supabase action, release decision or automatic next task.
