# Android V2 Motion UNVERIFIED And Blocked Ledger

Date: 2026-08-27

Purpose: keep unavailable proof, rejected directions, external dependencies,
and review-required architecture out of the active fix queue. An item in this
file must not prevent work on an independent Android V2 motion defect.

Status vocabulary:

- `REJECTED`: fresh evidence disproved the candidate or direction.
- `UNVERIFIED`: the requirement may be true, but required evidence is absent.
- `BLOCKED_EXTERNAL`: progress requires a device, user review, or external
  state that is currently unavailable.
- `REVIEW_REQUIRED`: implementation would materially change an approved
  architecture or visual layout and needs explicit written review.
- `READY_TO_RESUME`: the blocker is gone and the item can return to the active
  plan.

## Move-On Rule

1. Preserve exact evidence and current status.
2. Remove or disable any rejected product integration so it cannot become an
   accidental build.
3. Record the smallest condition that would allow resumption.
4. Move to the next independent item in the main handoff plan.
5. Never combine partial rows from rejected candidates into a synthetic PASS.

## Current Ledger

| ID | Item | Status | Evidence / reason | Resume condition |
| --- | --- | --- | --- | --- |
| UV-001 | Persistent portal-host Orb architecture | `REJECTED` | candidate29 never reached a canvas; candidate30 stopped after one size-120 frame; candidate31 stopped after two size-120 frames. Ledgers and rejection packets are under `output/android-motion-debug/2026-08-26-android-persistent-orb-candidate-{29,30,31}/`. | Do not resume this direction. |
| UV-002 | Unified same-parent Orb architecture | `REVIEW_REQUIRED` | It avoids WebView DOM-reparent/IntersectionObserver failure, but restructures select/refine layout ownership and carries a higher final-geometry regression risk. | User reviews the written same-parent design revision. |
| UV-003 | Current installed artifact state | `READY_TO_RESUME` | candidate42 was built only after benchmark Capacitor sync and `assembleBenchmark`. Host APK and pulled installed `base.apk` both match `a11071f04e0d166b00568b5bb8d30e72c7cdd5caa4965cf954238a08beb26fc8`; package/version are `com.zenflow.app / 2.1.1 / 38`, and the signing certificate is `c4a7cf3f…df75`. Immediate SHA gates remained exact through the Habits, Diary, Planning and Settings packets, including the restored App sound sequence. candidate42 is `FAIL`, not a promoted candidate, but it remains the exact current-source artifact for read-only/static correlation. | Re-bind candidate42 immediately before each independent local run; build a fresh numbered candidate before claiming any source fix. |
| UV-004 | Physical Android 12+ 60 Hz | `BLOCKED_EXTERNAL` | No physical device is attached. Emulator evidence is not physical/OEM evidence. | Attach an authorized physical Android 12+ 60 Hz device. |
| UV-005 | Physical Android 14+ 90/120 Hz | `BLOCKED_EXTERNAL` | No high-refresh physical device is attached. | Attach an authorized Android 14+ 90/120 Hz device. |
| UV-006 | Final user motion review | `BLOCKED_EXTERNAL` | No promoted final video exists because every new Orb candidate failed an earlier kill gate. | A candidate passes semantic, CDP, LayerTree, and Perfetto gates; then record and show the final videos. |
| UV-007 | Artistic/Craft acceptance | `FAIL` | Static visual guards pass, but the inline visual-integrity critic returned `STOP` for candidate42: Settings drawer/destination ownership and delayed day-background composition are visible craft failures. A valid continuous video, promoted final candidate and user review still do not exist. | Approved bounded ownership design, fresh candidate, valid continuous video, repeated visual-integrity review and actual user acceptance. |
| UV-008 | Perfetto for persistent Orb candidate | `UNVERIFIED` | candidates29–31 failed before promotion; running final Perfetto would have measured a rejected direction. | Run only for a candidate that passes continuous-motion and visual kill gates. |
| UV-009 | Full all-pages/all-controls matrix | `UNVERIFIED` | Orb portal work did not authorize claiming Habits, Diary, Planning, Settings, drawer, theme, IME, lifecycle, rotation, split-screen, reduced motion, or locale rows. | Continue each independent root-cause packet and mark rows only from fresh evidence. |
| UV-010 | Git publication and release | `BLOCKED_EXTERNAL` | User prohibited commit, push, merge, Play upload, and release. | New explicit authorization naming the exact external action. |
| UV-011 | Drawer/theme runtime root-cause selection | `READY_TO_RESUME` | candidate32 semantic Paper→Ink evidence is under `output/android-motion-debug/2026-08-27-current-source-drawer-candidate-32/`. The inactive full-screen day WebGL layer is absent in Ink; drawer layers fell `48→29`, drawing layers `41→23`, drawing-area ratio `9.545648→7.721352`, and the clean action log has no tile-memory warning. The visual video still fails because native status-bar icons switch before the WebView drawer frame. | Resume only after UV-013 design approval; candidate32 is not a visual PASS. |
| UV-012 | `codex_pixel_10_custom_api36` semantic theme action | `BLOCKED_EXTERNAL` | The AVD booted with SwiftShader due host memory pressure. Exact candidate20 and semantic `Open menu` were verified, but every later UIAutomator dump returned `null root`; `Dark` could not be activated through the required semantic node. Artifacts are under `output/android-motion-debug/2026-08-27-candidate20-swiftshader-drawer/`. | Use an API36 AVD/device where UIAutomator returns a stable tree; do not reuse SwiftShader performance rows. |
| UV-013 | Align Android native status-bar style with the committed WebView theme frame | `REVIEW_REQUIRED` | candidate32 frame-step evidence shows Paper at `9.10 s`, native status icons changing at `9.15–9.20 s` while the drawer is still Paper, and the Ink drawer only by `9.30 s`. `themeStore.applyToDOM()` changes the DOM and immediately calls `StatusBarStyle.setStyle()`, so native chrome outruns the asynchronous WebView compositor. Android documents `WebView.postVisualStateCallback()` specifically for waiting until current DOM, CSS animation, WebGL, and canvas state is ready for the next draw. | Approve the bounded Android-only design: gate `StatusBarStylePlugin` application through `postVisualStateCallback`, discard stale rapid-toggle callbacks, add RED→GREEN native contract tests, then build a fresh numbered APK and repeat the exact video. |
| UV-014 | Habits visual run invalidated by external APK replacement | `READY_TO_RESUME` | Immediately before the first CDP-off drawer→Habits recording, installed SHA matched candidate32 `fa963da1…0c62`. At `2026-08-27 00:50:20` another process installed `com.zenflow.app` version `2.0.0` code `35`, SHA `511c8c69…af00`; event log records `stop com.zenflow.app due to installPackageLI`. That first video/action/tree/log set remains INVALID. candidate32 was then reinstalled and passed four no-click SHA checks over 30 seconds plus before/after checks for `drawer-to-habits-retry.mp4`. | Keep immediate before/after SHA gates. If replacement repeats, stop runtime interaction on this shared serial and continue only non-runtime work or a separate task-owned target. |
| UV-015 | Phone drawer route stale-frame and blank-frame transition | `REVIEW_REQUIRED` | Valid exact-candidate32 `drawer-to-habits-retry.mp4` shows the drawer unchanged through `9.25 s`, a Habits spinner at `9.50 s`, stale Orb content already labelled Habits at `10.00 s`, an almost empty destination shell at `10.50 s`, and final Habits content at `11.00 s`. `useNavigationV2` explicitly defers route work through two rAF callbacks plus `120 ms`, then closes the drawer and publishes the page in a React transition; the WebView stall stretches that policy and the lazy boundary exposes stale/blank frames. | Approve the bounded generic navigation design: start destination preload on the semantic press, close the drawer immediately on its existing exit trajectory, wait for both preload readiness and drawer exit completion, then atomically publish the destination. Keep the current pixels, 300 ms drawer duration/easing, history, Back ownership, and route APIs; add RED→GREEN phone-navigation tests and repeat every drawer destination on a fresh APK. |
| UV-016 | Displayed/runtime app version is stale | `REVIEW_REQUIRED` | Exact candidate32/package metadata is `2.1.1` code `38`, but Settings and every module show `ZenFlow v2.0.0`. `src/lib/appVersion.ts` hard-codes `APP_VERSION = '2.0.0'` while `package.json` is `2.1.1`; the value also reaches update checks, device sessions, feedback, auth metadata, and other non-motion paths. | Treat as a separate version-provenance task. Do not change it inside the motion lane because the user prohibited storage, sync, API, and adjacent-platform changes here. Bind the runtime version to the canonical build version and test every consumer under separately approved scope. |
| UV-017 | Orb refine interaction-priority scheduling | `REJECTED` | candidate38 exact host/install SHA is `b023f253…716b`. Fresh CDP evidence restored mini-Orb presence but reached a complete refine scene only at `981.3 ms`, with `768 ms` partial-scene duration. No tile warning, ANR, context loss, skipped-frame or Davey message occurred, so the remaining failure is not explained by the deferred queue alone. Evidence and validated ledger are under `output/android-motion-debug/2026-08-27-orb-interaction-priority-candidate-38/`. | Do not resume queue-delay tuning. Remove the rejected integration and use only an approved non-scheduling ownership design. |
| UV-018 | Habits sheet ownership and Android IME focus visibility | `UNVERIFIED` | candidate39 proved that Vaul left Habits background controls exposed to UIAutomator; the page-content `inert`/`aria-hidden` owner fixed that in candidate40. candidate40 then proved unsolicited template autofocus: WebView height `190 px`, sheet `178 px`, `scrollTop=351`, form top `-256 px`, tile-memory warnings and severe jank. candidate41 fixed unsolicited autofocus, but a semantic manual focus reproduced the clipped/stale frame. candidate42 removed the delayed centered scroll, yet manual focus initially left the active input at `y=406…456` outside the `95 px` scroll clip; a later accessibility scroll moved it behind the fixed header, and 13 tile-memory warnings remained. A diagnostic-only clamp to the intersection of the Vaul scroller and visual viewport made the field fully visible at `y=103…152`, but did not reduce the `40.182954` drawing-area ratio or prove a source fix. Evidence is under `output/android-motion-debug/2026-08-27-habits-{modal-ownership-candidate-40,template-ime-candidate-41,manual-ime-candidate-42}/`. | Do not make a fourth focus-timing tweak. Resume only with a sheet-level, event-driven scroll-owner design plus an evidence-backed compositor ownership change that preserves pixels and removes tile-memory warnings; then build a new candidate and rerun semantic focus, Back precedence, CDP, video and Perfetto separately. |
| UV-019 | Diary route publication and editor layout under Android IME | `UNVERIFIED` | Exact candidate42 (`a11071…26fc8`) reproduced the generic drawer delay on Diary: the drawer remained visible with `Diary` highlighted, followed by `33/36/45/42` skipped-frame messages and `1281/1379/1500 ms` Davey frames before the destination settled. `New entry` then exposed a blank spinner frame before the empty editor appeared. Manual semantic focus did not emit tile warnings and LayerTree was only `10/7` with ratio `3.845899`, but at WebView height `190 px` the editor shell was also `190 px`; the scroll area and mobile Tools panel both began at `y=138.76`, so the active body at `y=122…382` was almost fully covered. First Android Back hid IME and kept the editor; second Back returned to Diary without saving. Evidence is under `output/android-motion-debug/2026-08-27-diary-editor-ime-candidate-42-run-01/` and in candidate42 ledger run `candidate42-diary-navigation-editor-ime`. | Resume with a shared Android-IME editor chrome/layout owner that preserves the normal editor pixels, leaves the active field visibly reachable, and does not reuse delayed centered scrolling. Rerun the same empty-editor path before any record-dependent Diary matrix. |
| UV-020 | Planning route ownership and unbounded timeline compositor surface | `UNVERIFIED` | Exact candidate42 reproduced a visible Planning loading row while the destination's controls were already exposed through UIAutomator behind the open drawer. The route then logged `32/47/54/33` skipped frames and `1421/1718/1538 ms` Davey frames. Settled LayerTree was `107/100`, ratio `68.066002`. Attributed layer `830` is the `ScheduleTimeline` horizontal scroller at `368928 × 315` physical pixels, about `116.2M` pixels. Source proves the geometry: `61` dates × `DAY_WIDTH_PX`, where `DAY_WIDTH_PX = 24 × 96 = 2304`, produces `140544 CSS px`; DOM day columns are windowed but the compositor scroll surface is not. Semantic `Focus` changed modes without data or new critical logs, yet the huge layer persisted (`111/104`, ratio `68.313129`). Evidence is under `output/android-motion-debug/2026-08-27-planning-modes-candidate-42-run-01/` and candidate42 ledger run `candidate42-planning-navigation-modes`. | Replace the full-width physical canvas with bounded logical virtual scrolling while preserving the visible 24-hour/day pixels, selection, RTL coordinates, keyboard access and scroll semantics. Add RED geometry/windowing tests, then verify LayerTree before any timer/event matrix. |
| UV-021 | Settings route/background/Back motion and Android switch accessibility | `FAIL` | Exact candidate42 left the drawer visible at `+2 s` after semantic Settings selection while accessibility already exposed destination controls, then emitted six tile-memory warnings and `1691/1564/1477/1358 ms` frames. Appearance controls preceded the canonical day background; Android Back still showed the detail at `+1 s` and recorded an `881 ms` frame. Sound itself opened by `+1 s` without a critical log marker, but UIAutomator exported enabled switches as `checkable=false / checked=false` while the DOM proved `aria-checked=true`. The completed `App sound ON→OFF→ON` run restored App sound/background/activity to `true` and vibration to `false`. Two invalid MP4s, two missing LayerTree snapshots and one overlapping shell UIAutomator failure are `UNVERIFIED_TOOLING`, not accepted app evidence. Packet: `output/android-motion-debug/2026-08-27-settings-controls-candidate-42-run-01/`; validated ledger run: `candidate42-settings-navigation-controls`. | Product/architecture review must choose bounded ownership for drawer publication, Settings day-background readiness, detail Back completion and Android accessibility-state export. Do not apply a broad visual patch in this lane; build a new candidate after approved RED tests. Repeat video only with a proven working recorder and keep the unvisited controls `UNVERIFIED`. |
| UV-022 | Current Android target availability after Settings closeout | `BLOCKED_EXTERNAL` | The last accepted Settings action retained exact candidate42 SHA before/after. At the final re-bind, `adb devices -l` returned no attached target and `adb shell pm path com.zenflow.app` failed with `no devices/emulators found`; the empty inventory is preserved at `output/android-motion-debug/2026-08-27-settings-controls-candidate-42-run-01/adb-devices-final.txt`. No emulator or external process was restarted. | Reattach the authorized target, then repeat devices/path/installed SHA/package/version checks before any runtime action. |

## Rejected Candidate Summary

| Candidate | APK SHA-256 | Result |
| --- | --- | --- |
| candidate29 | `b7d9782ddb7e09eaf1299d389a196a0c2435dc4dc5956460c2eceac86b698ace` | Loading surface remained; Worker/canvas count zero; `REJECTED`. |
| candidate30 | `3a481bc815ede98a291cc8d9b78e1beb49cb2dc132ed74b3058beb1b1bfb3aa2` | Same Worker/canvas and fast mini-Orb, but motion stopped after one refine frame; `REJECTED`. |
| candidate31 | `c30b40c9ff32fa0af76f1994e788f49237be94d4e86d189bc57ec324e329ef14` | Explicit resume produced only two refine frames before RAF stopped; `REJECTED`. |
| candidate38 | `b023f2532b05bf5a7e9e2a0ed363bb37ad95fe53c5fff2535be614e2170c716b` | Mini-Orb returned, but full refine scene took `981.3 ms` with `768 ms` staged pop-in; `REJECTED` before Perfetto/video. |
| candidate39 | `d7c00aaf3fd702139057139cd18b8bce2816163b78533b72c189c7a1faa67350` | Habits create sheet exposed background accessibility controls; `FAIL`. |
| candidate40 | `8f720db42e5149d1fbacb03c69bffe1eceed92d994e370277165b7b8d4706eb1` | Background ownership fixed, but template autofocus collapsed the sheet under IME and produced tile-memory warnings; `FAIL`. |
| candidate41 | `9657899785eff404d97c6de79f3156b81022b8e3ffd057ee742df6c66f1da986` | Template opens without unsolicited IME, but manual semantic focus clips the form and produces stale pixels/tile warnings; `FAIL`. |
| candidate42 | `a11071f04e0d166b00568b5bb8d30e72c7cdd5caa4965cf954238a08beb26fc8` | Habits IME remained clipped with tile warnings; Diary editor was covered under IME; Planning allocated a 116.2M-pixel timeline layer; Settings reproduced route/background/Back glitches and lost switch state in Android accessibility. Restored user preferences are proven, but no source fix was promoted; `FAIL`. |

## Active Queue After Move-On

1. Finish only safe read-only/static acceptance checks and the inline visual
   critic; do not activate more Settings controls.
2. Stop at UV-021/UV-015 product and architecture review boundaries.
3. Resume UV-018 only at its architecture/evidence boundary.
4. Resume UV-019 only at its architecture/evidence boundary.
5. Resume UV-020 only at its architecture/evidence boundary.
6. Resume UV-013 after bounded design approval.
7. Lifecycle/layout/locale and physical-device rows remain unverified or
   external until their runtime/user gates are available.

## Final Handoff Requirement

At the end of the main plan, report every row in this ledger with its final
status and the exact user/device/external action needed. Do not collapse this
file into a generic “remaining work” sentence.
