# ZenFlow - Контекст для Claude

> **Этот файл содержит всю необходимую информацию о проекте для продолжения работы в новом чате.**
> **Обновлено:** 2026-01-15

---

## 🎯 О проекте

**ZenFlow** — PWA-приложение для ментального здоровья и продуктивности, оптимизированное для людей с ADHD.

**Стек:**
- React 18 + TypeScript + Vite
- TailwindCSS + shadcn/ui
- Dexie.js (IndexedDB для локального хранения)
- Supabase (бэкенд, авторизация, облачная синхронизация)
- Capacitor (мобильные приложения iOS/Android)
- PWA с Service Worker

**Языки интерфейса:** ru, en, uk, es, de, fr

---

## ✅ Что уже сделано

### 1. Основной функционал
- [x] Трекер настроения (5 уровней, теги, заметки)
- [x] Трекер привычек (ежедневные, еженедельные, по расписанию, с таймером)
- [x] Фокус-таймер (Pomodoro, кастомные сессии, рефлексия)
- [x] Журнал благодарности
- [x] Статистика и аналитика (графики, хитмапы)
- [x] Система достижений и бейджей
- [x] Челленджи (streak, focus, gratitude)
- [x] Hyperfocus Mode с ambient sounds

### 2. Gamification & ADHD Hooks (НОВОЕ - 2026-01-15)
- [x] **Daily Login Rewards** — 7-дневный цикл наград с бонусом за streak
- [x] **Combo System** — множитель XP за последовательные действия (до 10x)
- [x] **Spin Wheel** — колесо удачи с призами
- [x] **Mystery Boxes** — сундуки с наградами (bronze/silver/gold/diamond)
- [x] **Flash Challenges** — срочные задания (15-60 мин)
- [x] **Time Challenges** — hourly/weekend челленджи
- [x] **Power-ups** — временные бонусы
- [x] **XP & Levels** — система прогрессии

**Файлы:**
- `src/lib/adhdHooks.ts` — вся логика ADHD-геймификации
- `src/hooks/useADHDHooks.ts` — React hook для состояния
- `src/components/DailyRewards.tsx` — UI ежедневных наград
- `src/components/ComboIndicator.tsx` — индикатор комбо
- `src/components/SpinWheel.tsx` — колесо удачи
- `src/components/FlashChallenge.tsx` — flash-челленджи
- `src/components/UserProgressBar.tsx` — прогресс-бар уровня
- `src/components/QuickActionsBar.tsx` — панель быстрых действий

### 3. Supabase Backend (НОВОЕ - 2026-01-15)
- [x] **SQL миграция** с полной схемой БД
- [x] **Row Level Security** на всех таблицах
- [x] **Real-time sync** подписки
- [x] **TypeScript типы** для Supabase

**Таблицы в Supabase:**
- `profiles` — профили пользователей
- `moods` — записи настроения
- `habits` — привычки
- `habit_completions` — выполнения привычек (нормализовано)
- `habit_reminders` — напоминания
- `focus_sessions` — фокус-сессии
- `gratitude_entries` — записи благодарности
- `user_settings` — настройки пользователя
- `challenges` — челленджи
- `badges` — полученные бейджи
- `adhd_state` — состояние геймификации (combo, spins, streak, xp)
- `mystery_boxes` — сундуки
- `time_challenges` — временные челленджи
- `user_backups` — полные бэкапы (legacy)
- `push_subscriptions` — push-подписки

**Файлы:**
- `supabase/migrations/001_initial_schema.sql` — SQL миграция
- `src/types/supabase.ts` — TypeScript типы
- `src/storage/realtimeSync.ts` — гранулярная синхронизация
- `src/lib/supabaseClient.ts` — клиент Supabase (типизированный)

### 4. Ambient Sounds
- [x] Реальные аудиофайлы (rain, ocean, forest, cafe, fireplace, white-noise)
- [x] Онлайн-фоллбеки с Pixabay CDN
- [x] Файлы в `public/sounds/`

### 5. Уведомления
- [x] Push notifications через Supabase Edge Functions
- [x] Локальные напоминания через Capacitor
- [x] Dopamine Dashboard — настройка обратной связи

### 6. i18n
- [x] 6 языков полностью переведены
- [x] ADHD hooks переводы добавлены во все языки

---

## 🔧 Что нужно доделать

### Высокий приоритет
1. **Интеграция ADHD компонентов в UI**
   - Добавить `UserProgressBar` в Header или Index
   - Добавить `QuickActionsBar` на главную страницу
   - Подключить `useADHDHooks` в Index.tsx
   - Показывать `DailyRewards` при первом входе за день
   - Показывать `SpinWheel` когда есть токены

2. **Применить SQL миграцию в Supabase**
   - Выполнить `supabase/migrations/001_initial_schema.sql` в Supabase Dashboard → SQL Editor
   - Или через CLI: `supabase db push`

3. **Подключить Real-time sync**
   - В Index.tsx вызывать `subscribeToRealtime()` после авторизации
   - Использовать `syncMood()`, `syncHabit()` и т.д. после мутаций данных

### Средний приоритет
4. **UI/UX улучшения**
   - Анимации для level up
   - Звуки для получения XP/наград
   - Haptic feedback на мобильных

5. **Оптимизация**
   - Code splitting (динамический импорт)
   - Lazy loading компонентов

### Низкий приоритет
6. **Premium функции**
   - Расширенная аналитика
   - Экспорт данных
   - Премиум темы

---

## 📁 Структура проекта (ключевые файлы)

```
src/
├── components/
│   ├── DailyRewards.tsx      # Ежедневные награды
│   ├── ComboIndicator.tsx    # Индикатор комбо
│   ├── SpinWheel.tsx         # Колесо удачи
│   ├── FlashChallenge.tsx    # Flash челленджи
│   ├── UserProgressBar.tsx   # Прогресс уровня (NEW)
│   ├── QuickActionsBar.tsx   # Быстрые действия (NEW)
│   ├── Header.tsx            # Шапка приложения
│   ├── StatsOverview.tsx     # Обзор статистики
│   ├── MoodTracker.tsx       # Трекер настроения
│   ├── HabitTracker.tsx      # Трекер привычек
│   ├── FocusTimer.tsx        # Фокус-таймер
│   └── ...
├── hooks/
│   ├── useADHDHooks.ts       # Hook для ADHD геймификации (NEW)
│   ├── useGamification.ts    # Основная геймификация
│   └── useIndexedDB.ts       # Hook для IndexedDB
├── lib/
│   ├── adhdHooks.ts          # Логика ADHD (NEW)
│   ├── ambientSounds.ts      # Ambient звуки
│   ├── supabaseClient.ts     # Supabase клиент
│   └── ...
├── storage/
│   ├── realtimeSync.ts       # Real-time sync (NEW)
│   ├── cloudSync.ts          # Облачная синхронизация
│   ├── db.ts                 # Dexie DB схема
│   └── backup.ts             # Экспорт/импорт
├── types/
│   ├── supabase.ts           # Типы Supabase (NEW)
│   └── index.ts              # Основные типы
├── i18n/
│   └── translations.ts       # Все переводы
└── pages/
    └── Index.tsx             # Главная страница

supabase/
└── migrations/
    └── 001_initial_schema.sql  # SQL миграция (NEW)

public/
└── sounds/                   # Аудио файлы
    ├── cafe-noise-32940.mp3
    ├── fireplace-fx-56636.mp3
    └── mixkit-*.wav
```

---

## 🔐 Переменные окружения

```env
VITE_SUPABASE_URL=https://xxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...
VITE_VAPID_PUBLIC_KEY=BG...
```

Файлы:
- `.env.development` — для локальной разработки
- `.env.production` — для продакшена

---

## 🚀 Команды

```bash
npm run dev      # Запуск dev сервера
npm run build    # Сборка для продакшена
npm run preview  # Превью сборки
```

---

## 📝 Незакоммиченные изменения

На момент создания этого файла:

**Измененные:**
- `src/i18n/translations.ts` — добавлены ADHD переводы
- `src/lib/supabaseClient.ts` — типизация

**Новые файлы:**
- `src/components/ComboIndicator.tsx`
- `src/components/DailyRewards.tsx`
- `src/components/FlashChallenge.tsx`
- `src/components/QuickActionsBar.tsx`
- `src/components/SpinWheel.tsx`
- `src/components/UserProgressBar.tsx`
- `src/hooks/useADHDHooks.ts`
- `src/lib/adhdHooks.ts`
- `src/storage/realtimeSync.ts`
- `src/types/supabase.ts`
- `supabase/migrations/001_initial_schema.sql`

**Нужно закоммитить:**
```bash
git add -A
git commit -m "feat: ADHD gamification + Supabase schema + realtime sync"
git push
```

---

## 🐛 Известные проблемы

1. **Bundle size warning** — index chunk > 600KB. Нужен code splitting.
2. **ADHD компоненты не интегрированы** — созданы, но не подключены к UI.

---

## 💡 Заметки для следующей сессии

1. **Первым делом:** закоммитить изменения (см. выше)
2. **Затем:** интегрировать ADHD компоненты в Index.tsx
3. **Проверить:** что SQL миграция применена в Supabase

---

*Этот файл обновляется при значительных изменениях в проекте.*
