{
  "$schema": "https://design-tokens.github.io/community-group/format/",
  "_meta": {
    "name": "ZenFlow Design Tokens",
    "version": "0.2.0-phase0b",
    "description": "W3C DTCG design tokens SSOT. Phase 0-B adds typography category (family, scale, weight, leading, tracking). Existing src/index.css HSL color vars remain the authoritative color source until Phase 2-B migration. Every token must exist here before being referenced in Tailwind/Components.",
    "spec": "https://design-tokens.github.io/community-group/format/ (2025-10-28)",
    "phase": "0-B-typography-system"
  },
  "color": {
    "design-system": {
      "$type": "color",
      "canary": {
        "$value": {
          "colorSpace": "oklch",
          "components": [0.58, 0.14, 240]
        },
        "$description": "Phase 0-A pipeline canary. Proves DTCG→Style Dictionary→PostCSS OKLCH→CSS fallback chain works end-to-end. DO NOT USE in production — this is infrastructure verification only."
      }
    }
  },
  "typography": {
    "family": {
      "$type": "fontFamily",
      "display": {
        "$value": ["Fraunces Variable", "Literata Variable", "Georgia", "Cambria", "Times New Roman", "serif"],
        "$description": "Display slot — entry titles, drop-caps, streak counters, quote watermarks. Phase 0-B.1 dual-font: Fraunces Variable (Latin+LatinExt, tri-axis opsz/SOFT/WONK/wght) for Latin scripts; Literata Variable (Cyrillic+CyrillicExt, opsz+wght) for Ukrainian/Russian text. Browser font-matching selects per glyph via unicode-range in src/styles/fonts.css — no JS required. Georgia stays as universal fallback (Law 17 i18n safety)."
      },
      "body": {
        "$value": ["Inter Variable", "-apple-system", "BlinkMacSystemFont", "SF Pro Text", "system-ui", "Roboto", "Helvetica Neue", "Arial", "sans-serif"],
        "$description": "Body slot — default UI text, paragraphs, inputs, buttons. Inter Variable covers Latin + LatinExt + Cyrillic + CyrillicExt. font-display: optional prevents FOUT flash on hot path."
      },
      "serif": {
        "$value": ["Fraunces Variable", "Literata Variable", "Georgia", "Cambria", "Times New Roman", "serif"],
        "$description": "Serif alias — same as display. Use for prose passages, editorial pull-quotes, bound-book export styling. Same Phase 0-B.1 Fraunces+Literata dual-font unicode-range cascade for Cyrillic coverage."
      },
      "hand": {
        "$value": ["Caveat Variable", "Comic Sans MS", "cursive"],
        "$description": "Hand slot — gratitude entries, intimate annotations, handwritten moments. Caveat Variable (Latin+Cyrillic, wght 400-700). Use sparingly — reserve for emotional moments per typography-grammar.md §Sacred Moments."
      },
      "mono": {
        "$value": ["ui-monospace", "SFMono-Regular", "Menlo", "Monaco", "Consolas", "monospace"],
        "$description": "Mono slot — reserved for tabular numerics (streak counters via font-feature-settings: 'tnum') and future code blocks. No bundled font this phase (system stack only)."
      }
    },
    "scale": {
      "$type": "dimension",
      "xs": {
        "$value": { "value": 0.75, "unit": "rem" },
        "$description": "12px — captions, metadata, footnotes."
      },
      "sm": {
        "$value": { "value": 0.875, "unit": "rem" },
        "$description": "14px — secondary UI, form hints, list metadata."
      },
      "base": {
        "$value": { "value": 1, "unit": "rem" },
        "$description": "16px — default body copy (WCAG 1.4.4 baseline)."
      },
      "lg": {
        "$value": { "value": 1.125, "unit": "rem" },
        "$description": "18px — lead paragraphs, editorial body."
      },
      "xl": {
        "$value": { "value": 1.25, "unit": "rem" },
        "$description": "20px — card titles, section intros."
      },
      "2xl": {
        "$value": { "value": 1.5, "unit": "rem" },
        "$description": "24px — sheet/modal titles."
      },
      "3xl": {
        "$value": { "value": 1.875, "unit": "rem" },
        "$description": "30px — page headings."
      },
      "4xl": {
        "$value": { "value": 2.25, "unit": "rem" },
        "$description": "36px — hero display."
      },
      "5xl": {
        "$value": { "value": 3, "unit": "rem" },
        "$description": "48px — drop-cap date, streak celebration moment."
      }
    },
    "weight": {
      "$type": "fontWeight",
      "regular": { "$value": 400, "$description": "Body copy default." },
      "medium": { "$value": 500, "$description": "Light emphasis, UI labels." },
      "semibold": { "$value": 600, "$description": "Headings, interactive emphasis." },
      "bold": { "$value": 700, "$description": "Strong emphasis, section titles." }
    },
    "leading": {
      "$type": "number",
      "tight": { "$value": 1.15, "$description": "Display/hero — compressed line-height for big sizes." },
      "snug": { "$value": 1.35, "$description": "Card titles, two-line labels." },
      "normal": { "$value": 1.5, "$description": "Body default — readable paragraph density." },
      "relaxed": { "$value": 1.7, "$description": "Long-form prose, journal entry reading." },
      "loose": { "$value": 1.9, "$description": "Gratitude handwriting, quote watermarks — breathing room." }
    },
    "tracking": {
      "$type": "dimension",
      "tight": {
        "$value": { "value": -0.02, "unit": "em" },
        "$description": "Display headlines — tighten for visual balance at large sizes."
      },
      "normal": {
        "$value": { "value": 0, "unit": "em" },
        "$description": "Body default."
      },
      "wide": {
        "$value": { "value": 0.05, "unit": "em" },
        "$description": "Small caps, tabular labels."
      },
      "wider": {
        "$value": { "value": 0.1, "unit": "em" },
        "$description": "Emphasis labels, small UI text."
      },
      "widest": {
        "$value": { "value": 0.2, "unit": "em" },
        "$description": "Section kickers (all-caps, tiny)."
      }
    }
  },
  "space": {
    "_placeholder": {
      "$value": "Phase 0-B will populate 8-point spacing scale (0, 4px, 8px, 12px, 16px, 20px, 24px, 32px, 40px, 48px, 64px, 80px, 96px, 128px).",
      "$type": "string"
    }
  },
  "radius": {
    "_placeholder": {
      "$value": "Phase 0-B will populate border-radius scale (0, 2px, 4px, 8px, 12px, 16px, 24px, 9999px).",
      "$type": "string"
    }
  },
  "shadow": {
    "_placeholder": {
      "$value": "Phase 0-B will populate elevation scale (sm, md, lg, xl, 2xl, inner, glow, soft, card, hover).",
      "$type": "string"
    }
  },
  "motion": {
    "duration": {
      "_placeholder": {
        "$value": "Phase 0-B will populate duration scale (instant 0ms, fast 150ms, base 250ms, slow 400ms, slower 600ms, slowest 1000ms).",
        "$type": "string"
      }
    },
    "easing": {
      "_placeholder": {
        "$value": "Phase 0-B will populate easing curves (linear, ease-in, ease-out, ease-in-out, spring, bounce).",
        "$type": "string"
      }
    }
  },
  "z-index": {
    "_placeholder": {
      "$value": "Phase 0-B will populate z-index scale (base 0, dropdown 10, sticky 20, nav 50, modal 60, overlay 70, tooltip 80, toast 90).",
      "$type": "string"
    }
  }
}
