/**
 * HabitsPage — Phase 3-C scroll-linked single page (Hero / Garden / MindMap).
 *
 * The implementation lives in `./habits/HabitsPage` so that the page can be
 * decomposed into zone modules without growing this entry point. Re-export
 * keeps existing imports (e.g. `NavV2Orchestrator`) stable.
 */
export { HabitsPage } from "./habits/HabitsPage";
