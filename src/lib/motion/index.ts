/**
 * Barrel export for the motion verb grammar.
 *
 * Consumers should import from `@/lib/motion` rather than individual files:
 *
 *   import { verbs, gated, Bloom, Fold, Breathe } from '@/lib/motion';
 */

export * from './verbs';
export * from './components';
