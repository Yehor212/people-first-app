/**
 * Barrel export for motion verb React components.
 * Ripple is NOT a wrapper — use `createRipple()` from `@/lib/motion` inside
 * an onPointerDown handler instead (ripple spawns a transient DOM node).
 */

export { Bloom, type BloomProps } from './Bloom';
export { Fold, type FoldProps } from './Fold';
export { Breathe, type BreatheProps } from './Breathe';
