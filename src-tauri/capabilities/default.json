{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default ZenFlow desktop capability. Keep least-privilege until a feature-specific security review adds more permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

