[package]
name = "zenflow-desktop"
version = "2.0.0"
description = "ZenFlow desktop runtime"
authors = ["ZenFlow"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.6.3", features = [] }

[dependencies]
tauri = { version = "2.11.3", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
