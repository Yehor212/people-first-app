# Push-уведомления в ZenFlow

## ✅ Что уже работает

### 1. **Мобильные приложения (Android/iOS)**
Push-уведомления **полностью настроены и работают** через Capacitor Local Notifications:

- ✅ Индивидуальные уведомления для каждой привычки
- ✅ Настройка времени и дней недели для каждой привычки
- ✅ Автоматическая синхронизация при изменении привычек
- ✅ Поддержка нескольких напоминаний для одной привычки

**Локация кода:**
- `src/lib/localNotifications.ts` - функция `scheduleHabitReminders()`
- `src/pages/Index.tsx` - интеграция (строки 408-413)

### 2. **Web-версия (PWA)**
Для веб-версии работают **Web Push Notifications** через service worker:

- ✅ PWA настроена и работает
- ✅ Service worker регистрируется автоматически
- ✅ Пользователь может разрешить уведомления в браузере

**Локация кода:**
- `vite.config.ts` - конфигурация PWA
- Service worker генерируется автоматически при билде

## 📱 Как работают уведомления

### На мобильных устройствах:
1. При первом запуске приложение запрашивает разрешение на уведомления
2. Компонент `NotificationPermission` показывает красивый экран с объяснением
3. Когда пользователь добавляет/изменяет привычку с напоминаниями, автоматически планируются локальные уведомления
4. Уведомления приходят даже когда приложение закрыто

### В веб-версии:
1. Браузер запрашивает разрешение на уведомления
2. PWA service worker обрабатывает фоновые уведомления
3. Работает в Chrome, Firefox, Edge (не работает в Safari на iOS)

## 🔧 Настройка для каждой привычки

Пользователь может настроить напоминания в компоненте `HabitTracker`:

```typescript
// Структура напоминания
interface HabitReminder {
  enabled: boolean;
  time: string;      // "09:00"
  days: number[];    // [1,2,3,4,5] (Пн-Пт)
}
```

**Пример:**
- Напоминание 1: Каждый день в 08:00
- Напоминание 2: Пн-Пт в 20:00
- Напоминание 3: Сб-Вс в 10:00

## 🚀 Что НЕ требует дополнительной настройки

- ✅ **Ничего!** Все уже настроено и работает
- ✅ Capacitor Local Notifications установлен
- ✅ Permissions уже запрашиваются
- ✅ Уведомления автоматически обновляются при изменении привычек

## 📊 Тестирование

### Мобильные (Android/iOS):
```bash
# Соберите приложение для мобильной платформы
npm run build
npx cap sync

# Запустите на Android
npx cap run android

# Запустите на iOS
npx cap run ios
```

### Веб:
1. Откройте приложение в Chrome
2. Нажмите F12 → Application → Service Workers
3. Проверьте, что service worker активен
4. Разрешите уведомления в браузере

## 🔐 Безопасность

- Все уведомления **локальные** (не требуют сервера)
- Никакие данные не отправляются на внешние серверы
- Полная приватность пользователя

## 📝 Примечания

1. **Safari на iOS:** Web Push работает только с iOS 16.4+ и требует добавления PWA на домашний экран
2. **Android:** Работает во всех браузерах без ограничений
3. **Desktop:** Chrome, Firefox, Edge полностью поддерживают Web Push

## 🎯 Итог

**Push-уведомления полностью готовы к продакшену!** Ничего дополнительно настраивать не нужно. Все работает автоматически как на мобильных, так и в веб-версии.
